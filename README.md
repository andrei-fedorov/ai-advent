# TooManyRules — день 18

Ассистент по правилам настольной игры Too Many Bones (Chip Theory Games).
Без RAG (он появится на неделе 5) — модель отвечает из общих знаний,
чтобы позже наглядно сравнить с версией, которая читает реальные правила.

С дня 6 вся работа с LLM вынесена в отдельную сущность — **агента**.
Агент владеет конфигом (системный промпт, модель, режим рассуждения,
температура, лимиты), сам ведёт стек сообщений, сам ходит в API, считает
токены и стоимость и пишет лог. Интерфейс — только вид на его состояние:
содержимое чата на каждом шаге рендерится из `agent.history`, своей копии
переписки Gradio не ведёт.

С дня 7 диалог переживает перезапуск приложения: история лежит на диске
по файлу на сессию, и при старте процесса агенты поднимаются из этих
файлов. Открытая страница сразу показывает тот же диалог — кнопки
«восстановить» в интерфейсе нет.

С дня 8 агент считает токены **до** запроса, а не только после: видно, из
чего складывается запрос, сколько это от контекстного окна модели и как
токены со стоимостью растут по ходам. Партия в Too Many Bones идёт несколько
часов, и каждый следующий вопрос тащит в модель весь предыдущий разговор —
день 8 показывает цену этого свойства в токенах и деньгах.

С дня 9 агент этой ценой **управляет**. Последние N сообщений уходят в модель
как есть, всё, что старше, сворачивается в короткую сводку — отдельным
служебным вызовом модели, который виден в панели и оплачен по той же таблице
цен. Стратегия переключается на живом диалоге, и это ключевое: стек сообщений
и файл сессии всегда остаются полными, сжимается **запрос, а не память**.
Поэтому один и тот же разговор можно отправить несколькими способами и
сравнить ответы.

День 10 добавляет ещё два способа собрать запрос и один способ разветвить
сам диалог:

- **«Скользящее окно»** — в модель уходят только последние N сообщений,
  всё старше отбрасывается из запроса без замены. Ни одного служебного
  вызова, запрос не растёт — плата идёт деталями, которые выпали из окна,
  а не токенами.
- **«Факты + последние N»** — отдельный блок «ключ — значение»
  (`цель`, `состав`, `коробки`, `герои`, `тиран`, `договорённости`,
  `поправки игрока` и т.д.), который служебный вызов обновляет **перед
  каждым ответом**. Дороже сводки по числу вызовов (два вызова на каждый
  ход вместо одного раз в несколько ходов), но не пересказывает, а хранит
  факты — и не размывается пересказом того, что уже не изменится.
- **Checkpoint и ветка диалога** — не пункт списка стратегий (у веток
  расходится сама история, а не способ её отправить), а отдельная сессия:
  «Сохранить checkpoint» фиксирует конец текущего диалога вместе со снимком
  памяти всех стратегий, «Ветка от checkpoint'а» порождает от этой точки
  новый независимый диалог со своим файлом, своей стратегией и своими
  счётчиками. От одного checkpoint'а можно завести сколько угодно веток —
  например, чтобы сравнить «а что, если бы мы выбрали другого тирана»,
  не смешивая варианты партии в одном разговоре.

Сравнение всех четырёх стратегий на одном сценарии — в
[`docs/TooManyRules — День 10 сравнение стратегий.md`](docs/TooManyRules%20—%20День%2010%20сравнение%20стратегий.md).

День 11 открывает неделю 3 и даёт агенту **модель памяти из трёх слоёв**,
разведённых по времени жизни. Партия заканчивается, игрок остаётся: коллекция
коробок, «отвечай коротко, я с телефона» и домашнее «спорное — в пользу
игроков» верны неделями, а герои, тиран и «в 21:00 надо закончить» — один вечер.

- **Краткосрочная** — разговор этой сессии: стек сообщений и стратегии
  дней 9-10. Лежит в файле сессии, уходит по «Сбросить диалог».
- **Рабочая** — данные текущей партии (цель, состав, герои, тиран,
  ограничения, договорённости партии, открытые вопросы). Перед каждым ответом
  её пополняет служебный вызов «разбор памяти»; лежит в том же файле сессии,
  в `context.working`, копируется в ветку и уходит со сбросом.
- **Долговременная** — игрок вообще: окружение (коллекция, игровая группа),
  решения (домашние правила) и знания (что игрок сообщил о правилах как
  проверенное). Лежит в отдельном файле `src/data/memory/long_term.json`,
  общем для всех сессий и агентов, и пишется **только с согласия человека**:
  разбор предлагает кандидатов, сохраняет их кнопка.

Какой ключ в какой слой — решает не модель, а **карта памяти** в
`presets.py`; переключатель «Слои памяти в запросе» меняет то, что уходит в
модель, а не то, что агент помнит. Проверка слоёв на сценарии из двух
сессий — в
[`docs/TooManyRules — День 11 проверка слоёв памяти.md`](docs/TooManyRules%20—%20День%2011%20проверка%20слоёв%20памяти.md).

День 12 добавляет ещё одну сущность — **профиль пользователя**, и это ни
пресет, ни память. Пресет выбирает разработчик — это параметры вызова модели
(роль, температура, лимиты). Профиль пишет сам пользователь в редакторе — это
как с ним работать: обращение, язык, уровень, ограничения и **режимы** под
задачу («За столом», «Учу новичка», «Разбор после партии»). Память выводится
из разговора и предлагается моделью; профиль модель не пишет и не предлагает
править никогда. Подробности — в разделе
[«Профиль пользователя» ниже](#профиль-пользователя-не-память-и-не-пресет-день-12).

День 13 даёт агенту **состояние задачи** — конечный автомат с этапом
(`планирование → выполнение → проверка → готово`), текущим шагом, ожидаемым
действием и планом с отметками. Партия — это данные («герои Patches и
Picket, тиран Drellen»), задача — это ход работы («шаг 3 из 5, шаги 1-2
выполнены, ждём подтверждения»), и ни одно в другое не пишется: рабочая
память (день 11) помнит первое, состояние задачи — второе.

- **Этап и шаг меняет только таблица переходов** (`task_state.TRANSITIONS`),
  а не модель: событие для перехода называет **трекер** — служебный вызов
  перед ответом — по разговору, или человек кнопками; допустим ли переход,
  решает код.
- **Пауза — не этап, а флаг** на любом из трёх активных этапов. Ставится и
  снимается словами (трекер понимает «давай прервёмся» и «мы вернулись») или
  кнопками «⏸ Пауза»/«▶ Продолжить». На паузе трекеру доступно только
  «продолжить», вопросы задачу не двигают.
- **Задачу начинает и отменяет только человек** — кнопками «Начать задачу с
  этим сообщением» и «Отменить задачу»; одна задача на сессию.
- **Блок состояния уходит в запрос сразу за рабочей памятью**, поэтому пауза
  и продолжение не зависят от того, что осталось в окне контекста —
  проверено на «Скользящем окне» намеренно тесным (см. документ проверки).
  Отказ трекера этого хода тоже попадает в блок, чтобы ассистент объяснил
  его пользователю, а не сделал вид, что переход состоялся.

Состояние и журнал переходов лежат в `context.task` файла сессии, переживают
перезапуск, копируются в ветку из снимка checkpoint'а и очищаются сбросом —
тем же приёмом, что рабочая память дня 11. Проверка паузы на каждом этапе и
продолжения с блоком и без него — в
[`docs/TooManyRules — День 13 проверка состояния задачи.md`](docs/TooManyRules%20—%20День%2013%20проверка%20состояния%20задачи.md).

День 14 даёт агенту **инварианты** — ограничения, которые он не вправе
нарушить, о чём бы ни просили в разговоре. Это не системный промпт (его пишет
разработчик и он один на всех), не профиль (он говорит, **как** отвечать),
не память (**что** агент знает) и не состояние задачи (**где** мы в работе):
инвариант говорит, **чего нельзя**. Пишет их только человек, в редакторе.

- **Две области.** `всегда` — файл `src/data/invariants/invariants.json`,
  общий для всех сессий и агентов, рядом с памятью и профилем; `сессия` —
  `context.invariants` файла сессии, копируется в ветку, уходит со сбросом.
- **Блок в запросе — первым из блоков агента**, сразу за системным промптом:
  инвариант устойчивее всего остального, поэтому окно контекста его не
  вытесняет, а просьба «забудь про ограничения» — повод отказать, а не
  основание нарушить.
- **Страж** — пятый служебный вызов вокруг того же `_call_service_model()`:
  перед ответом сверяет новое сообщение с инвариантами и называет конфликт.
  Ход при этом не отменяется и ничего не блокируется — отказывает сам
  ассистент, а код только **показывает** конфликт (статус хода, блок
  «Инварианты», журнал ходов, лог).
- **Инвариант над переходом автомата** — единственное место, где нарушение
  ловит код: `запрещён` (переход не состоится ни от трекера, ни от кнопки) или
  `с подтверждением` (переход трекера ждёт кнопки «✅ Подтвердить переход»).
  Таблица переходов дня 13 при этом не меняется — ограничение фильтр поверх
  неё.

Ответ модели код не проверяет: день даёт видимость, а не гарантию. Как
держатся инварианты на живом сценарии — прямой конфликт, давление,
инвариант при пустом окне, ветка без блока и оба режима над переходом — в
[`docs/TooManyRules — День 14 проверка инвариантов.md`](docs/TooManyRules%20—%20День%2014%20проверка%20инвариантов.md).
Подробности — в разделе
[«Инварианты» ниже](#инварианты-не-память-не-профиль-и-не-системный-промпт-день-14).

День 15 делает контроль переходов **жёстким** — «красный путь» поверх
готового автомата дня 13. День 13 вёл задачу по маршруту прямой дорогой
(happy path); за столом так не бывает: «давай без плана», «сразу к пятому
шагу», «проверки не нужны, считай, что готово», «игнорируй все стадии».

- **Недопустимый переход виден и не проходит.** Трекер теперь видит **все**
  события задачи с пометкой «можно сейчас» / «сейчас нельзя» и называет то,
  чего просит пользователь; допустимость решает код по таблице. Отказ — с
  причиной: строка «Не состоялось» уходит в блок запроса этого хода,
  ассистент объясняет, а этап и шаг не сдвигаются.
- **Сход с маршрута** — отметка трекера о просьбе, которой в таблице нет
  вовсе («игнорируй все стадии», «проверки не нужны»): вид `пропустить этап`
  или `игнорировать порядок` и фраза словами пользователя. Сход ничего не
  меняет в состоянии и хода не отменяет, но виден в блоке запроса (с
  указанием не выполнять просьбу в обход этапов), статусе, панели, журнале
  ходов и логе. Шестого служебного вызова у дня нет — сход отмечает трекер,
  который и так идёт каждым ходом активной задачи.
- **«Сейчас нельзя»** — вторая половина блока: запреты этапа («нельзя
  реализацию до утверждённого плана», «нельзя финал без валидации»,
  «на паузе задачу не двигать») выводит код, а не модель, в том месте
  запроса, которое не вытесняется окном контекста.
- **Откат назад по графу** — два новых перехода: «вернуться к плану»
  (словами или кнопкой «↩ Вернуться к плану»; отметки и итоги шагов стираются,
  план остаётся черновиком, выполнение потом начинается с шага 1) и
  «переоткрыть» (только кнопкой «↩ Переоткрыть задачу», только с «готово», к
  проверке 1). Откат — такой же переход, с записью в журнале, а не «начать
  заново»; отменённая задача не переоткрывается.
- **Маршрут проверяется при импорте `task_state.py`**: вперёд по основному
  пути — не больше чем на один этап, все активные этапы достижимы, тупиков
  нет, конечные этапы конечны. «Перепрыгнуть этап нельзя» — свойство таблицы,
  а не обещание.

Состояние задачи не сдвинуть ничем, кроме допустимого перехода; ответ
ассистента код не проверяет — день даёт видимость попытки, а не гарантию,
что модель не поддастся. Как это держится на живом сценарии (недопустимые
переходы, реакция ассистента, продолжение после паузы, оба отката, ограничение
дня 14 над новым переходом, ветка без блока) — в
[`docs/TooManyRules — День 15 проверка переходов.md`](docs/TooManyRules%20—%20День%2015%20проверка%20переходов.md).
Подробности — в разделе
[«Жёсткий контроль переходов» ниже](#жёсткий-контроль-переходов-красный-путь-день-15).

День 16 открывает неделю 4 — **MCP** (Model Context Protocol). У ассистента
появляется первый внешний источник — BoardGameGeek через открытый MCP-сервер
[bgg-mcp](https://github.com/kkjdaniel/bgg-mcp), и сегодня приложение честно
показывает только его **каталог**: само запускает сервер, согласует с ним
протокол и получает список инструментов. Блок «MCP: сервер BoardGameGeek» —
наверху дебаг-панели: кнопка, статус соединения (сервер, версия протокола,
как договорились, возможности, время), таблица инструментов с параметрами и
ответ `tools/list` целиком. Агент инструментами пока не пользуется и о
сервере не знает — это следующий шаг; в запрос сегодня не уходит ничего.
Подробности и сборка сервера — в разделе
[«MCP: сервер BoardGameGeek» ниже](#mcp-сервер-boardgamegeek-день-16).

День 17 — **первый инструмент MCP**, которым пользуется агент. Сервер свой:
`src/faq_server.py` на том же SDK v2 (`MCPServer`, stdio) вокруг
**официального FAQ издателя по Too Many Bones** на портале поддержки Chip
Theory Games. Два инструмента — `faq_questions` (список вопросов, с фильтром по
разделу) и `faq_article` (текст статьи). Агент получает их через function
calling DeepSeek и **сам решает**, сверяться ли с FAQ: на вопрос о правилах
модель просит список вопросов, открывает подходящую статью, пересказывает
ответ издателя по-русски и даёт ссылку; на «привет» инструменты не зовёт.
Один ход становится несколькими раундами «модель → вызовы инструментов →
модель», в историю идут только вопрос и финальный ответ. Вызовы и раунды
последнего хода — в блоке «MCP: свой сервер FAQ» наверху дебаг-панели,
переключатель «Инструменты MCP в запросе» выключает всё это — запрос как на
дне 16, ответ из общих знаний. Подробности — в разделе
[«Свой MCP-сервер FAQ» ниже](#свой-mcp-сервер-faq-день-17).

День 18 — **планировщик и фоновые задачи**. У ассистента появляется служба,
которая работает круглосуточно и без приложения: **сторож FAQ**
(`src/faq_watch.py`) — долгоживущий MCP-сервер по Streamable HTTP. По
расписанию (по умолчанию раз в сутки) он снимает официальный FAQ издателя
целиком в SQLite, сравнивает снимок с прошлым и **после каждого снимка сам
пишет сводку изменений** в свой терминал: какие статьи появились, изменились,
переехали в другой раздел, пропали. Два его инструмента агент получает вместе
с инструментами FAQ дня 17: `faq_changes` отдаёт агрегированную сводку за
период прямо из базы, без запросов к сайту, а `faq_watch_schedule` меняет
расписание — ответ сразу, снимок по новому расписанию потом, в фоне. На «что
нового в FAQ за месяц?» ассистент зовёт сводку и пересказывает её со ссылками,
на «проверяй раз в час» — меняет расписание. Запускает сторож человек
(`./run.sh faq-watch`), а не приложение; приложение без него работает как на
дне 17. Подробности — в разделе [«Сторож FAQ» ниже](#сторож-faq-день-18).

## Что видно в приложении

Один экран без вкладок:

- **Слева — чат.** Многоходовый диалог: второй вопрос отвечает с учётом
  первого, потому что стек сообщений держит агент. Кнопка «Сбросить диалог»
  очищает стек и удаляет файл сессии, не сбрасывая счётчики агента (а с
  дня 10 — и checkpoint'ы с происхождением ветки, если они были); кнопка
  «Новый агент» поднимает ещё один экземпляр с текущим пресетом, а «Удалить
  агент» убирает активного из процесса вместе с его файлом (интерфейс
  переходит на оставшегося).
- **Сверху — выпадающий список пресетов.** Смена пресета создаёт **нового
  агента** с другим конфигом и пустым стеком — в том же процессе.
- **Под ним — переключатель стратегии контекста**, теперь на четыре пункта:
  «Вся история», «Сводка + последние N» (день 9), «Скользящее окно» и
  «Факты + последние N» (день 10). Соседний список с другой семантикой, и
  это подписано прямым текстом: пресет создаёт нового агента, а стратегия
  нового агента **не** создаёт и стек **не** трогает — меняется только то,
  что из истории уходит в модель начиная со следующего вопроса.
- **Ещё ниже — «Ветка диалога»** (день 10): третий переключатель с третьей
  семантикой — не новый агент, не другой запрос у того же агента, а **другая
  история**. Показывает исходный диалог и все его ветки; переключение меняет
  и стек, и файл, и стратегию, и счётчики на те, что у выбранной сессии.
- **Ещё ниже — «Инструменты MCP в запросе»** (день 17): первый из
  переключателей того, что уходит в модель, — инструменты не блок сообщений,
  а отдельный параметр запроса. Включено — в начале каждого хода приложение
  запрашивает каталог у серверов FAQ и (с дня 18) сторожа FAQ, и модель сама
  решает, вызывать ли инструменты; выключено — запрос как на дне 16: ни каталога, ни схем, один
  раунд. Нового агента не создаёт и ничего не пишет; у нового,
  восстановленного агента и у ветки — включён.
- **Ещё ниже — «Слои памяти в запросе»** (день 11): флажки «долговременная»
  и «рабочая». Выключенный слой не уходит в модель, но продолжает
  записываться — разбор памяти идёт при любом положении, и включённый обратно
  слой сразу полон. Нового агента переключатель не создаёт и ничего не пишет
  на диск; краткосрочную память урезает стратегия.
- **Ещё ниже — «Инварианты в запросе»** (день 14): выключенный блок не уходит
  в модель, но страж продолжает сверять сообщения и показывать конфликты, а
  ограничения переходов продолжают действовать; выключить сам инвариант можно
  только флажком «Действует» в редакторе. Меняет запрос, а не работу; у
  нового, восстановленного агента и у ветки — включён.
- **Ещё ниже — «Профиль в запросе»** (день 12): `авто` (режим на этот ход
  выбирает роутер — служебный вызов модели), название режима (в запрос
  всегда уходит он) или `выключен` (профиль в запрос не уходит, сам профиль
  не трогается). Переключатель не создаёт агента и ничего не пишет — меняет
  запрос, а не профиль; у нового, восстановленного агента и у ветки всегда
  `авто`.
- **Ещё ниже — «Состояние задачи в запросе»** (день 13): выключенный блок не
  уходит в модель, но трекер продолжает вести задачу, и включённый обратно
  блок сразу актуален. Начинают и завершают задачу не этим переключателем, а
  кнопками под чатом.
- **Под полем ввода — сценарий дня 18**: «Н1 · что нового за месяц», «Н2 ·
  раз в час», «Н3 · когда проверка», «Н4 · вопрос о правилах». Клик кладёт
  вопрос в поле, отправляет человек; сторож FAQ для Н1-Н3 должен быть запущен
  (`./run.sh faq-watch`). Сценарии прошлых дней, в том числе И1-И4 дня 17, —
  в закрытом аккордеоне ниже.
- **Под кнопками «Отправить / Сбросить диалог / Новый агент / Удалить
  агент» — ряд кнопок задачи** (день 13): «Начать задачу с этим сообщением»
  (текст поля ввода становится целью), «⏸ Пауза», «▶ Продолжить», «Отменить
  задачу», а с дня 14 — «✅ Подтвердить переход». С дня 15 «Отменить
  задачу» переехала во второй ряд — назад по графу: «↩ Вернуться к плану»,
  «↩ Переоткрыть задачу», «Отменить задачу». Кнопки активны всегда —
  недопустимое нажатие отклоняет автомат, и в статусе видно почему
  («📋 Задача: отклонено «переоткрыть» — переоткрывают только завершённую
  задачу»); подтверждать нечего — тоже отказ со словами в статусе.
- **Под строкой статуса — редактор инвариантов** (день 14; с дня 15 в
  закрытом аккордеоне «… (день 14)»): список «Инвариант для правки» («+ новый» и книга агента), формулировка
  (не больше 25 слов), область, необязательный переход автомата с режимом,
  флажок «Действует», кнопки «Сохранить инвариант» и «Удалить инвариант» и
  заготовки. Форма, как редактор профиля: её поля не входят в общий вид на
  состояние агента. Смена области переносит инвариант (номер меняется).
- **Под строкой статуса — редактор профиля**, закрытый аккордеон: общая
  часть («как обращаться», «язык», «уровень», «ограничения») и режим для
  правки с полями «когда», «стиль», «формат», «ограничения», «порядок
  ответа». Единственная форма в интерфейсе — её поля не входят в общий вид
  на состояние агента, иначе любое действие затирало бы несохранённую
  правку. Кнопка «Сохранить профиль» проверяет профиль и пишет его в файл,
  общий для всех агентов и сессий; примеры «А · опытный игрок» / «Б ·
  новичок» заполняют общую часть одним кликом.
- **Под кнопками чата — ряд checkpoint'ов** (день 10): «Сохранить
  checkpoint» фиксирует конец текущей истории вместе со снимком памяти всех
  стратегий (с дня 11 — и рабочей памяти), список «Checkpoint» показывает
  checkpoint'ы активной сессии, а «Ветка от checkpoint'а» создаёт от
  выбранного новую сессию с копией общего начала истории — она сразу
  становится активной.
- **Под ним — ряд кандидатов** (день 11): «Кандидаты в долговременную
  память» с кнопками «Сохранить в долговременную» и «Отклонить». Пункт —
  ключ, предложенное значение и что было раньше; по умолчанию не отмечено
  ничего. Когда кандидаты есть, статус хода начинается с них: человеку есть
  что решить.
- **Справа — дебаг-панель.** Наверху, развёрнут — блок **«Сторож FAQ (день
  18)»**: адрес сервера и как его запустить, где его база, кнопка «🔌
  Подключиться и получить список инструментов» (статус соединения, таблица
  двух инструментов с описаниями параметров, ответ `tools/list` целиком),
  поле «Дней» и кнопка **«📰 Сводка сторожа за N дней»** — ответ `faq_changes`
  дословно, тот же текст, что получила бы модель; сторож не запущен — сбой
  стадии «соединение» и подсказка `./run.sh faq-watch`. Под ними —
  **«Инструменты последнего хода агента (все серверы)»**: каталог хода и
  размер схем (с дня 18 — и ⚠️, если один из серверов не ответил), таблица
  вызовов (раунд, инструмент, аргументы, результат, время), раунды с
  `finish_reason` и `prompt`, предупреждения (ошибка инструмента, обрезка
  результата, потолок раундов, каталог не получен) и в закрытом аккордеоне —
  ровно то, что ушло модели сообщениями `tool`. Под ним, свёрнуты, — блоки
  **«MCP: свой сервер FAQ (день 17)»** (чем запускается сервер, кнопка
  каталога — протокол 2026-07-28 через `server/discover`, без рукопожатия) и
  **«MCP: сервер BoardGameGeek (день 16)»**: те же кнопка, статус и таблица
  для bgg-mcp; результат кнопок живёт только в этой вкладке до перезагрузки
  страницы.
  Ниже, свёрнутыми аккордеонами: конфиг агента (`AgentConfig`), стек сообщений
  (растёт на 2 за ход), метрики последнего вызова (время, `finish_reason`,
  токены, стоимость, оценка против факта, кэш промпта, служебный вызов этого
  хода — свёртка или обновление фактов, в зависимости от стратегии, разбор
  памяти с дня 11, роутер профиля с дня 12, трекер задачи с дня 13 и **страж
  инвариантов** с дня 14), **«Инварианты»** (день 14: книга с номерами `П1…` и
  `С1…`, областью и пометкой перехода, сколько действует, последний конфликт
  с тем, что просил пользователь, блок в запросе и его размер, где лежат
  постоянные и сессионные, будет ли страж на следующем ходе), **«Профиль»** (день 12: где
  лежит файл, положение переключателя у этого агента, режим прошлого хода
  и режим следующего запроса с размером блока, общая часть и все режимы с
  пометкой активного и размером блока каждого), **«Слои памяти»** (день 11:
  по слою — что в нём лежит, где, в запросе ли он; рабочая память по
  ключам, долговременная по разделам «окружение / решения / знания» с
  сессией и временем записи, кандидаты, ждущие решения, и ключи вне карты
  последнего разбора), **«Состояние задачи»** (день 13: цель, этап на пути
  `планирование → выполнение → проверка → готово` с пометкой паузы, текущий
  шаг, ожидаемое действие, план с отметками ✓/→, допустимые сейчас события
  отдельно для человека и трекера, последний переход и последний отказ,
  размер блока в следующем запросе и полный журнал переходов; с дня 15 —
  ещё «Сейчас нельзя», последний сход с маршрута и счётчики красного пути;
  блок называется «Состояние задачи и маршрут (дни 13, 15)» и с дня 16 свёрнут
  вместе с таблицей автомата),
  **«Контекст: что
  уходит в модель»** (стратегия,
  состав запроса, что осталось за бортом и чем заменено, экономия и итог
  «сэкономлено против потрачено на служебные вызовы»), **«Память
  стратегии»** (сводка или факты текстом — подпись зависит от активной
  стратегии), **бюджет контекста** (с дня 12 — отдельной строкой корзина
  профиля, с дня 13 — корзина задачи, с дня 14 — инвариантов, первой из
  блоков), **рост по ходам** (таблица ходов
  агента, с дня 10 — со временем ответа и служебного вызова, с дня 12 —
  режим хода, корзина профиля и токены/время роутера, с дня 13 — этап и шаг
  после хода, событие перехода, корзина и время трекера, с дня 14 — корзина
  инвариантов, номера конфликта, токены и время стража, с дня 17 — раунды,
  вызовы инструментов, корзина схем и время инструментов; «оценка/факт»
  сравнивает оценку с `prompt` первого раунда), **«Агенты
  процесса»** (день 10: расход по
  состоявшимся ходам каждого агента реестра рядом, без подсветки «лучшего»;
  с дня 12 — ещё и положение переключателя профиля и токены роутера, с
  дня 13 — этап/шаг задачи и токены трекера, с дня 14 — сколько инвариантов
  действует и токены стража),
  накопленное агентом за время жизни (с дня 12 — отдельной строкой роутер
  профиля, с дня 13 — трекер задачи, с дня 14 — страж инвариантов) и
  счётчики процесса.
  `reasoning_content` показывается свёрнутым блоком, когда модель его вернула.
  Выше переключателя агентов — блок **«Ветки диалога»** (день 10): что это
  за сессия (исходный диалог или чья-то ветка, жива ли связь с родителем),
  какие у неё checkpoint'ы и какие ветки от них уже созданы, и всё семейство
  сессий целиком. Под стеком сообщений — блок **«Хранилище»** (сессия, путь
  к файлу, сколько сообщений восстановлено при создании агента), **сырое
  содержимое файла сессии** и рядом с ним — **файла долговременной памяти**
  (день 11), **файла профиля** (день 12) и **файла инвариантов** (день 14; три
  последних — в закрытом аккордеоне): рабочая память лежит в первом,
  долговременная — во втором, профиль — в третьем, инварианты — в четвёртом, и
  четыре файла рядом и есть «инварианты хранятся отдельно от диалога».
- **Переключатель агентов** над стеком сообщений: в нём все агенты, поднятые
  с момента старта процесса (в том числе восстановленные из файлов) и ещё не
  удалённые. Выбор агента делает его активным: переключаются чат,
  стек, конфиг и метрики, и следующий вопрос уходит ему. Так видно, что
  инстансов действительно несколько и переписка у каждого своя; с дня 10
  подпись агента-ветки дополнена происхождением (`#6 · Базовый · s006 ·
  ветка от s005 · 14 сообщ.`).
- **Под чатом — шесть блоков примеров.** Три вопроса дня 6, одиннадцать
  сообщений сценария сравнения стратегий (день 10: шаги 1-6, контрольные
  вопросы К1-К3, вторая ветка В2 и общий вопрос КВ), одиннадцать сообщений
  сценария проверки слоёв памяти (день 11: первая партия С1-С6 и К1-К3,
  следующая партия Н1 и Н3; Н2 — это К2), девять сообщений сценария
  проверки профиля (день 12: вопрос В1 для сравнения профилей и режимов,
  сообщения Р1-Р8 для роутера) и семнадцать сообщений сценария проверки
  состояния задачи (день 13: З1-З9, В1-В6, Г и Д) — клик кладёт текст в поле
  ввода, отправляет человек (З1 — кнопкой «Начать задачу с этим сообщением») —
  и пятнадцать сообщений сценария проверки инвариантов (день 14: Н1-Н2, К1-К4,
  Д1-Д2, В1-В3, Т1-Т4) и четырнадцать сообщений сценария проверки
  переходов (день 15: Р1-Р3, П1-П7, О1, Г и Д, Б1; Р1 — кнопкой «Начать
  задачу», Б1 — только в ветке без блока). У дня 16 сценария в чате нет — его
  инструмент в дебаг-панели, — поэтому все шесть блоков свёрнуты в аккордеон
  «Примеры и сценарии прошлых дней (6, 10-15)».

Каждый вызов дополнительно логируется в stdout с номером агента, именем
пресета и сессией (`[#2 Базовый · s002]`) — терминал, где запущен `run.sh`,
остаётся основным инструментом отладки. Туда же идёт каждое обращение к
диску: `сохранено s002: 4 сообщ. → src/data/sessions/s002.json`, — и с дня 16
каждое подключение к MCP-серверу с префиксом `[MCP bgg]`: запуск, соединение,
`tools/list` с именами инструментов, закрытие или сбой со стадией.

### Пресеты

Один и тот же агент, разные конфиги — то, что на неделе 1 было разными
кусками кода:

| Пресет | Конфиг | Откуда |
| --- | --- | --- |
| Базовый | `SYSTEM_PROMPT`, `deepseek-flash`, thinking off | день 1 |
| Строгий JSON | промпт с требованием JSON, `max_tokens=350`, `stop=["###END###"]` | день 2 |
| Креативный | `SYSTEM_PROMPT`, `temperature=1.2` | день 4 |
| Флагман + thinking | `SYSTEM_PROMPT`, `deepseek-v4-pro`, thinking on | день 5 |

## История диалогов на диске

Одна сессия — один файл `src/data/sessions/sNNN.json`:

```json
{
  "version": 2,
  "session_id": "s001",
  "preset": "Базовый",
  "created_at": "2026-09-09T14:15:30",
  "updated_at": "2026-09-09T14:22:07",
  "context": {
    "strategy": "Сводка + последние N",
    "memory": {
      "Сводка + последние N": {
        "summary": "Играем базовой коробкой вдвоём, герои Пэтч и Тантрум…",
        "covered": 12,
        "updated_turns": 2
      }
    }
  },
  "messages": [
    {"role": "user", "content": "Из каких фаз состоит ход игрока?"},
    {"role": "assistant", "content": "Ход игрока состоит из..."}
  ]
}
```

Формат версии **2** с дня 9: рядом с историей на диск едет память стратегии.
Файлы версии 1 читаются как есть — нет ключа `context`, значит пустая память
и стратегия по умолчанию; миграций нет, первая же запись делает файл вторым.

**Формат не растёт и на дне 10.** Checkpoint'ы и происхождение ветки едут в
тот же непрозрачный блок `context` — новыми ключами `checkpoints` (у сессии,
где сохраняли checkpoint) и `branch` (у сессии, которая сама является
веткой), только непустые:

```json
"context": {
  "strategy": "Факты + последние N",
  "memory": { "Факты + последние N": {"facts": {"тиран": "Drellen"}, "covered": 14, "updated_turns": 8} },
  "checkpoints": [
    {"id": "cp1", "messages": 12, "strategy": "Факты + последние N", "memory": {"…": "снимок памяти на момент сохранения"}, "created_at": "2026-09-13T19:35:03"}
  ]
}
```

```json
"context": {
  "strategy": "Вся история",
  "memory": {},
  "branch": {"parent": "s005", "checkpoint": "cp1", "messages": 10, "created_at": "2026-09-13T19:35:03"}
}
```

**Не растёт формат и на дне 11.** Рабочая память — состояние сессии и едет
в тот же блок `context` ключом `working` (только непустая), а в каждый
checkpoint — своим снимком `working`:

```json
"context": {
  "strategy": "Скользящее окно",
  "memory": {},
  "working": {
    "entries": {"цель": "подготовиться к сегодняшней партии", "состав партии": "вдвоём с женой", "тиран": "Drellen"},
    "covered": 16,
    "updated_turns": 9
  }
}
```

Долговременная память с сессиями не связана, поэтому лежит **не в файле
сессии**, а отдельно — `src/data/memory/long_term.json`, сосед каталога
сессий. У каждой записи — сессия и время, когда человек её сохранил:

```json
{
  "version": 1,
  "updated_at": "2026-09-16T01:12:06",
  "entries": {
    "коллекция": {"value": "дома только базовая коробка, Undertow не наш — он у друга", "session_id": "s003", "updated_at": "2026-09-16T01:11:44"},
    "домашние правила": {"value": "спорное место в правилах всегда трактуем в пользу игроков", "session_id": "s003", "updated_at": "2026-09-16T01:10:45"}
  }
}
```

Файлы дня 10 кодом дня 11 читаются без предупреждений (ключа `working` нет —
рабочая память пуста). Кандидатов в долговременную память и положения
переключателя слоёв нет ни в одном файле.

Что важно знать:

- **Файл появляется с первой записью.** У обычной сессии это первый
  успешный ответ; у ветки (день 10) — момент создания, вместе с уже
  скопированной историей. Исчезает по «Сбросить диалог»: пустых файлов
  сессий не бывает — нет контекста, нет и сессии. Запись атомарная
  (временный файл рядом плюс `os.replace`), файл каждый раз переписывается
  целиком.
- **При старте процесса** приложение читает каталог и поднимает по агенту на
  каждую сессию — по возрастанию `session_id`, то есть в порядке создания.
  Сколько восстановлено, видно в логе старта и в строке статуса на странице.
- **На диск едут только история и имя пресета.** Системный промпт, модель и
  параметры остаются в коде (`presets.py`) и подтягиваются по имени пресета:
  правка промпта доезжает до восстановленных агентов. Имени нет в наборе —
  берётся пресет по умолчанию, приложение не падает.
- **Каталог данных не коммитится** (`src/.gitignore`): переписка и
  долговременная память — локальные данные. Путь переопределяется переменной
  окружения `TOOMANYRULES_DATA_DIR` — так поднимается второй экземпляр
  приложения, не перемешивая свою историю с рабочей. Долговременная память
  такого экземпляра ложится рядом с его каталогом сессий
  (`<каталог>/memory/`) и в память автора не пишет:

  ```bash
  TOOMANYRULES_DATA_DIR=/tmp/tmr-test GRADIO_SERVER_PORT=7861 python app.py
  ```

- **Приложение поднимается при любом состоянии каталога:** каталога нет, он
  пуст, в нём битый JSON или посторонний файл. Нечитаемая сессия пропускается
  с предупреждением в лог, её файл не перезаписывается, остальные
  восстанавливаются. Агент, не прочитавший свою историю, выключает себе
  запись — чтобы не затереть файл, который сегодня не прочитался.
- **Сбой записи не роняет ход:** ответ отдаётся как обычно, причина видна в
  логе и в блоке «Хранилище», а на следующем ходе запись повторяется.
- Метрики и счётчики на диск **не** сохраняются: восстанавливается контекст
  диалога, а не бухгалтерия — «потрачено» относится к запуску приложения.
  Сводка на диск едет именно потому, что она контекст: после перезапуска она
  на месте, и повторной свёртки не происходит. А журнал ходов и «сэкономлено»
  начинаются заново — это бухгалтерия. С дня 11 так же ведут себя рабочая и
  долговременная память (память, на месте после перезапуска) и кандидаты
  (вопрос к человеку в этом процессе, после перезапуска их нет).
- **Битый файл долговременной памяти не затирается:** память стартует
  пустой, запись в неё выключается, причина видна в логе и в панели. Сбой
  записи оставляет кандидатов ждать решения.

## Токены: оценка до запроса и факт после

С дня 5 токены считались только **после** вызова и только за один вызов — из
`usage` в ответе API. День 8 добавляет вторую сторону: сколько уйдёт в модель
**до** того, как мы туда сходим, из чего это складывается и сколько от лимита
модели уже занято.

- **Оценка и факт стоят рядом, и это принципиально.** Точное число до запроса
  дал бы только тот же токенизатор, что стоит на сервере модели: он
  раздаётся демо-зипом и тянет `transformers` — сотни мегабайт в проект с
  тремя строчками в `requirements.txt`, а `tiktoken` считает по правилам
  OpenAI, то есть не то, за что выставят счёт. Поэтому в `tokens.py` —
  эвристика по классам символов (кириллица / латиница / прочее; пробельные не
  считаются вовсе), а рядом всегда показан факт из `usage`.
- **Калибровка по собственному трафику.** У нас есть то, чего нет у
  токенизатора: после каждого вызова известно, сколько сервер насчитал ровно
  за ту строку, которую мы отправили. Агент копит отношение «факт / оценка» и
  применяет его к бюджету; первые же ходы подтягивают оценку к факту. На
  живом диалоге из 8 ходов на русском стартовые коэффициенты дали +6.7%
  расхождения (замер записан комментарием в `tokens.py`), калибровка снимает
  и это.
- **Бюджет контекста.** Окно модели берётся из таблицы `CONTEXT_WINDOW_TOKENS`
  и уменьшается на резерв под ответ (`max_tokens` из конфига, иначе
  константа): «занято 100%» не должно означать, что ответу места уже не
  осталось. Уровни занятости — `ok` / `warn` / `danger` / `over`, полоса и
  предупреждение в панели. Модели нет в таблице (локальная модель недели 6) —
  «н/д», без процентов и без падений, ровно как `estimate_cost_usd()`.
- **Рост по ходам.** Агент ведёт журнал успешных ходов, и панель показывает
  его таблицей: ход, стек до хода, оценка, `prompt`/`completion`/`total`,
  расхождение оценки с фактом, стоимость хода и накопительно; с дня 9 —
  стратегия хода, сколько сообщений ушло в модель, сколько ушло бы со всей
  историей и служебные токены, с дня 10 — время ответа и время служебного
  вызова. Главное, что
  видно — `prompt` растёт от хода к ходу, хотя вопросы одинаково короткие:
  n-й ход тащит в модель все предыдущие. Журнал ведёт себя как счётчики агента, а не
  как стек сообщений: «Сбросить диалог» его не чистит, на диск он не едет и
  после перезапуска пуст, хотя история восстановлена.

### Что происходит при переполнении

У поломки два конца, и в проекте есть оба:

- **на выходе** — `max_tokens` обрывает ответ на полуслове с
  `finish_reason=length`; это живёт в пресете «Строгий JSON» с дня 2;
- **на входе** — контекстное окно. С API это не «модель забудет начало
  диалога», а **отказ целиком**: ответа нет, ход не состоялся, стек сообщений
  и файл сессии не изменились, счётчик ошибок вырос.

Агент отправляет запрос, даже когда сам считает, что тот не влезет:
предупреждение в панели и в логе есть, предохранителя нет — и день 9 его не
добавил. Сжатие это способ до переполнения **не доходить**, а не проверка
перед вызовом.

Достать переполнение вручную нельзя — окно моделей V4 больше миллиона
токенов, столько не наговорить. Для этого в панели есть кнопка **«Набить
контекст»**: она кладёт в поле вопроса текст-заполнитель нужного размера и
ничего не отправляет — отправляет человек обычной кнопкой «Отправить».
Заполнитель, который **прошёл** в модель, остаётся в истории и дорожает с
каждым следующим ходом, так что после эксперимента диалог сбрасывается.

Размер окна — внешний факт, и он сверен с сервером: документация DeepSeek
говорит «1M», а переполненный запрос возвращает `400` с текстом
`This model's maximum context length is 1048576 tokens`. В таблице стоит
число из ошибки, а не из документации.

## Управление контекстом: четыре стратегии и одна ветка

День 8 показал проблему и ничего не починил: каждый ход тащит в модель весь
предыдущий разговор. День 9 эту кривую ломает первым способом — сводкой; день
10 добавляет ещё два способа собрать запрос («Скользящее окно», «Факты +
последние N») и один способ разветвить саму историю (checkpoint и ветка). Все
четыре стратегии сборки запроса и переключатель между ними в интерфейсе живы
на живом диалоге — и честно показывают, чем за приём платят.

### Сводка + последние N (день 9)

Как это работает:

- Параметры — константы `presets.py`: последние **6** сообщений уходят как
  есть, свёртка наступает, когда несвёрнутого хвоста набралось ещё на **6**
  сообщений сверх них (то есть впервые — на 12 сообщениях, дальше каждые
  3 хода). Длину сводки держит просьба в промпте — не длиннее **150** слов, а
  потолок служебного вызова (**800** токенов) — только страховка от обрыва на
  полуслове. Лимит в предложениях длину не держал: модель соблюдала число
  предложений и растягивала каждое.
- **Свёртка — отдельный вызов модели.** Сворачиваемые сообщения уходят текстом
  внутри одного `user`-сообщения (иначе модель отвечает на последний вопрос
  игрока вместо того, чтобы сворачивать диалог), с собственным системным
  промптом и всегда выключенным thinking. Предыдущая сводка — часть входа: на
  выходе получается **одна** сводка, покрывающая и её, и новые сообщения.
- **В запрос сводка встаёт ведущим `system`-сообщением** сразу после
  системного промпта и помечена как пересказ. Дальше идёт весь несвёрнутый
  хвост — `history[covered:]`, а не «последние 6»: сообщение не может выпасть
  из запроса, не попав перед этим в сводку.
- **Сводка хранит то, чего модель не знает.** Не пересказ формулировок
  правил, а какая игра и дополнение, каким составом играют, какие правила
  разобраны и **к чему пришли**, о чём договорились. Выводы обязательны:
  книги правил у модели нет до недели 5, Too Many Bones она знает плохо, и
  поправка игрока — ровно то, чего в её весах нет.
- **История не режется ни в стеке, ни в файле.** Сводка живёт отдельным полем
  рядом с полной перепиской.

### Скользящее окно (день 10)

Самая простая стратегия: в модель уходят только последние **6** сообщений
истории, всё старше отбрасывается из запроса **без замены** — ни служебного
вызова, ни пересказа. Плата — деталями: на прогоне сравнения к контрольному
вопросу о времени партии второе сообщение, где оно было названо, уже выпало
из окна, и модель назвала своё («примерно 2–3 часа» вместо «в 21:00 надо
закончить»), а на вопрос о договорённости из третьего сообщения ответила, что
договорённостей не было. Зато ни одного лишнего вызова, и запрос перестаёт
расти, как только диалог длиннее окна.

### Факты + последние N (день 10)

Отдельный блок «ключ — значение» (`цель`, `состав`, `коробки`, `герои`,
`тиран`, `договорённости`, `поправки игрока`, `открытые вопросы` и т.д.),
который служебный вызов обновляет **перед каждым ответом** — не пересказ, а
выжимка того, что понадобится, когда исходных сообщений в контексте уже не
будет.

- **Источник факта — только игрок.** Утверждения ассистента о правилах
  фактами не становятся, пока игрок их не подтвердил: книги правил у модели
  нет, Too Many Bones она знает плохо, и собственная ошибка, записанная в
  факты, подтверждала бы сама себя в каждом следующем запросе. Это главное
  правило промпта фактов. Остальные формулировки промпта подобраны на живом
  сценарии, и какой сбой закрывает каждая, записано в комментарии у
  `FACTS_PROMPT` в `presets.py`; замер итогового промпта — у
  `FACTS_MAX_TOKENS`.
- **Ответ модели — только изменения**, разбираются построчно в `context.py`:
  `ключ: значение` — новый или изменившийся факт, `ключ: удалить` — отмена,
  `нет изменений` — успешное обновление без правок. Поправка игрока — это
  новое значение ключа, а не удаление. Слияние, а не замена: факт, который
  модель забыла повторить, не теряется. Разбор прощает оформление: выделение
  и кавычки вокруг строки, ключа или значения срезаются, «нет изменений» с
  пояснением тоже понимается — иначе «**тиран**» встал бы отдельным ключом
  рядом с «тиран».
- **Платит служебным вызовом на каждом ходе** — дороже сводки по числу
  вызовов, но исправление (поправка игрока) обновляет тот же ключ на месте,
  а не тонет в пересказе шестью ходами позже.

### Checkpoint и ветка диалога (день 10)

Ветка — **не пункт списка стратегий**, а отдельная сессия: у веток расходится
сама история, а не способ её отправить, и внутри любой ветки работает любая
из четырёх стратегий.

- **«Сохранить checkpoint»** фиксирует конец текущей истории вместе со
  снимком памяти всех стратегий. Сами сообщения не копируются — история от
  checkpoint'а не укорачивается, а снимок памяти согласован с историей только
  в момент сохранения.
- **«Ветка от checkpoint'а»** порождает новый агент с новой сессией: история —
  копия префикса до checkpoint'а, память стратегий и активная стратегия — из
  снимка. Файл ветки пишется сразу, не после первого ответа: история у неё
  уже есть.
- **Копия, а не ссылка.** Сброс или удаление исходного диалога ветку не
  ломает — панель честно пишет, что связь с родителем потеряна. От одного
  checkpoint'а можно завести сколько угодно веток, и они не видят решений
  друг друга: на прогоне сравнения ни одна из двух веток не упомянула выбор
  соседней. Оговорка того же прогона: ветка с Drellen этого тирана не
  приняла и сама подставила Nom, так что на общий контрольный вопрос обе
  ветки назвали одного тирана (документ сравнения, §5).
- **Checkpoint не опережает историю.** Нажатый во время ответа, он
  дожидается конца хода: иначе в снимок попала бы память, уже обновлённая по
  вопросу, которого в истории ещё нет.

### Чем платит каждая стратегия

- **Сводка** — оплачивается сразу, а экономится потом. На коротком диалоге
  сжатие дороже, чем его отсутствие, и панель пишет это прямым текстом.
- **Окно** — платит деталями: что выпало из окна, модели узнать неоткуда.
- **Факты** — платят вызовом на каждом ходе, и это видно в журнале ходов
  числом (не только словом): в «Росте по ходам» у каждого хода две последние
  колонки — время ответа и время служебного вызова.
- **Ветки** — платят ручным управлением: сами по себе они ничего не сжимают,
  а checkpoint и ветку создаёт человек.
- **Служебный вызов считается наравне с обычными** — те же счётчики агента и
  процесса, та же оценка стоимости, та же строка в логе. Но это не ход: в
  стек сообщений он не пишет, строку журнала ходов не создаёт и метрики
  последнего вызова не затирает — там остаётся ответ игроку.
- **Сбой служебного вызова хода не отменяет.** Память не двигается, ответ
  приходит, причина видна в панели и в логе, а запрос собирается тем, что
  есть: деградация идёт в сторону «без сжатия», а не в сторону потерянного
  контекста. Обновление остаётся назревшим и повторится на следующем ходе.
- **Обрезанный ответ не проходит молча.** Если служебный вызов упёрся в
  потолок (`finish_reason=length`), результат применяется — иначе задача
  повторялась бы каждый ход за деньги, — но в логе предупреждение, а в панели
  и статусе хода пометка.

В логе служебный вызов виден одной строкой, и по ней понятно, что она дала —
формат общий для сводки и фактов, слова приходят от стратегии:

```
[#3 Базовый · s003] служебный вызов — свёртка сообщений #0-#5 (6 шт.) в сводку: учтено ≈1 436 токенов истории → сводка ≈257 токенов (сводка переписана, учитывает 6 сообщ.); 2.50s, tokens=2096/270/2366, cost=$0.000476
[#1 Базовый · s001] служебный вызов — обновление фактов по сообщениям #6-#7 (2 шт.) и новому сообщению: учтено ≈289 токенов истории → факты ≈192 токенов (~коробки; +открытые вопросы); 1.10s, tokens=1356/48/1404, cost=$0.000232
[#5 Базовый · s005] checkpoint cp1: 10 сообщ., стратегия «Вся история», снимок памяти стратегий: нет памяти
[#6 Базовый · s006] агент создан как ветка от s005 · cp1: общий префикс 10 сообщ., стратегия «Вся история», память стратегий из снимка checkpoint'а; живых агентов: 6
```

Заполнитель дня 8 при этом остаётся обычным сообщением истории и попадает под
общие правила стратегии — тяжёлое сообщение либо схлопывается в сводку, либо
выпадает из окна, либо превращается в несколько строк фактов.

Реальный прогон сценария на всех четырёх стратегиях и на ветках — числа,
дословные ответы и содержимое файлов — в
[`docs/TooManyRules — День 10 сравнение стратегий.md`](docs/TooManyRules%20—%20День%2010%20сравнение%20стратегий.md).

## Модель памяти: три слоя (день 11)

До дня 11 память агента была устроена по одному признаку — что отправить в
модель из разговора, — и вся исчезала вместе с разговором. День 11 разводит её
**по времени жизни**: разговор, партия, игрок.

| | Краткосрочная | Рабочая | Долговременная |
| --- | --- | --- | --- |
| Что это | разговор этой сессии | данные текущей партии | игрок вообще: окружение, решения, знания |
| Живёт | сессию | сессию — она же партия | не зависит от сессий и агентов |
| Где лежит | `src/data/sessions/sNNN.json` → `messages` | тот же файл → `context.working` | `src/data/memory/long_term.json` |
| Кто пишет | агент, каждым ходом | разбор памяти, сразу | разбор предлагает кандидата, записывает человек |
| В запросе | стратегия контекста | ведущим `system`-блоком | ведущим `system`-блоком, первым после системного промпта |

Сводка и факты дней 9-10 — не отдельный слой, а способ отправить краткосрочную
память короче.

### Карта памяти: слой выбирает не модель

`MEMORY_MAP` в `presets.py` — закрытый список ключей, у каждого слой, раздел и
описание:

- **рабочая часть** («партия»): `цель`, `состав партии`, `герои`, `тиран`,
  `сложность`, `ограничения`, `договорённости партии`, `открытые вопросы`;
- **долговременная часть**: `коллекция`, `игровая группа` (окружение),
  `домашние правила` (решения), `знания о правилах` (знания). Ключ `формат
  ответов` был здесь до дня 12 — как отвечать теперь профиль пользователя,
  а не память (подробности ниже).

Граница между слоями — «верно для этой партии» против «верно для игрока
вообще», а не тема: «договорённости партии» и «домашние правила», «состав
партии» и «игровая группа» — в разных слоях. Модель по описанию выбирает
**ключ**, слой ключа берётся из карты, ключ вне карты отклоняется и виден в
логе и панели.

### Разбор памяти

Перед каждым ответом — ещё один служебный вызов модели: на входе рабочая и
долговременная память, ждущие решения кандидаты, новые сообщения и новое
сообщение игрока; на выходе — только изменения в формате фактов дня 10
(`ключ: значение`, `ключ: удалить`, `нет изменений`), разбор строк общий с
фактами. Рабочие ключи сливаются в рабочую память сразу, долговременные
становятся кандидатами. Источник записи — только игрок: утверждения ассистента
о правилах записью не являются; какой сбой на живом сценарии закрывает каждое
правило промпта, записано у `MEMORY_PROMPT`. Разбор идёт при любом положении
«слоёв в запросе», считается в счётчиках агента и процесса, но **не** в
служебных вызовах стратегий — он ничего не экономит, он добавляет; в панели и
журнале ходов у него свои строки и колонки. Сбой разбора ход не отменяет:
память не двигается, разбор повторится на следующем ходе.

### Долговременная запись — только с подтверждения

Ошибка в рабочей памяти живёт до конца партии; ошибка в долговременной уходит
в каждый запрос каждой будущей сессии. Поэтому в долговременную память
попадает только то, что человек отметил в ряду кандидатов и сохранил кнопкой.
Кандидат — не память, а вопрос к человеку: в запрос не уходит, на диск не
пишется, в ветку не копируется, перезапуск не переживает; сказанное игроком
от отклонения не теряется — оно в истории разговора. На прогоне проверки
разбор один раз предложил в `знания о правилах` собственный вывод о правилах,
которого игрок не говорил, — кнопка «Отклонить» не пустила его в память
(документ проверки, §2).

### Слои в запросе, ветки и сброс

- **Переключатель «Слои памяти в запросе» меняет запрос, а не память** — тем
  же правилом, что стратегии дня 9. Положение переключателя — состояние
  агента, но не диалога: на диск не едет, у нового, восстановленного агента и
  у ветки оба слоя включены.
- **В запросе слои стоят от устойчивого к изменчивому:** системный промпт,
  долговременная память, рабочая, память стратегии, хвост истории, вопрос. В
  бюджете контекста и журнале ходов у слоёв свои корзины, а экономия стратегии
  считается без них.
- **Ветка** получает копию рабочей памяти из снимка checkpoint'а и ту же
  долговременную память; **сброс** чистит краткосрочную и рабочую, а
  долговременную не трогает; **удаление агента** уносит его разговор и
  рабочую память вместе с файлом сессии.

В логе слои видны теми же строками с номером агента и сессией:

```
[#3 Базовый · s003] разбор памяти по сообщениям #8-#9 (2 шт.) и новому сообщению: рабочая: без изменений; ждут подтверждения: коллекция, знания о правилах; вне карты: нет; finish_reason=stop 1.42s, tokens=1 896/51/1 947, cost=$0.000315; ответ: '…'
[#3 Базовый · s003] кандидат в долговременную память: коллекция = «дома только базовая коробка, Undertow не наш — он у друга» (было: «дома есть базовая коробка и Undertow») — ждёт решения
[#3 Базовый · s003] долговременная память: отклонено — знания о правилах = «Patches и Picket доступны в базовой коробке, для игры без Undertow подходят» (раньше ключа не было); в память не попало, сказанное осталось в истории разговора
[#3 Базовый · s003] долговременная память: сохранено 1 — коллекция; решение человека → src/data/memory/long_term.json
[#2 Базовый · s002] слои в запросе: рабочая → нет; стек и память не тронуты
```

Проверка слоёв на сценарии из двух сессий (первая партия со слоями и без,
следующая партия с долговременной памятью и без неё, ветка и сброс) — числа,
дословные ответы и оба файла — в
[`docs/TooManyRules — День 11 проверка слоёв памяти.md`](docs/TooManyRules%20—%20День%2011%20проверка%20слоёв%20памяти.md).

## Профиль пользователя: не память и не пресет (день 12)

После дня 11 агент знает мир игрока, но нигде не записано **как ему
отвечать**: во время боя нужен вердикт в пять строк, когда игрок объясняет
игру новичку — шаги и пример, после партии — разбор подробно. День 12 даёт
агенту профиль — явные настройки, которые пользователь пишет сам, а не
выводит модель.

| | Пресет | Профиль | Долговременная память |
| --- | --- | --- | --- |
| Кто задаёт | разработчик, в `presets.py` | пользователь, в редакторе | модель предлагает, человек подтверждает |
| Что описывает | как вызывать модель: роль, температура, лимиты, формат ответа | как работать с этим пользователем: обращение, уровень, режимы под задачу | что верно о мире пользователя: коллекция, домашние правила, знания |
| Где лежит | в коде; на диск едет только имя | `src/data/profile/profile.json` | `src/data/memory/long_term.json` |
| Как меняется | правкой кода; смена — новый агент | кнопкой «Сохранить профиль»; действует со следующего хода у всех агентов | кнопкой «Сохранить в долговременную» |
| Может ли изменить модель | нет | нет | только предложить |
| В запросе | системный промпт | блок сразу за системным промптом | блок после профиля |

### Общая часть и режимы

Профиль состоит из двух уровней. **Общая часть** верна для любой задачи:
обращение, язык ответов и названий, уровень в игре, ограничения во всех
ответах — все поля необязательные. **Режим** — профиль под одну задачу:
название, когда он нужен (по этому описанию выбирает роутер), стиль,
формат, ограничения и **порядок ответа** — шаги, из которых складывается
ответ. В запрос уходят общая часть и **один** режим, выбранный на этот
ход, — не все режимы сразу: иначе указания разных режимов конфликтовали бы
в одном запросе, а какой режим сработал, не было бы видно ни в логе, ни в
панели.

Профиль по умолчанию — три режима задачи игрока:

| Режим | Когда | Формат ответа |
| --- | --- | --- |
| **За столом** | партия идёт, нужен быстрый ответ по ситуации | не больше пяти пунктов, вердикт первым |
| **Учу новичка** | объясняете игру тому, кто не играл | пошагово, нумерованным списком, с примером |
| **Разбор после партии** | партия закончилась, есть время разобрать подробно | связный текст с подзаголовками |

Формат режима «За столом» — это бывший ключ памяти `формат ответов` дня 11,
перенесённый туда, где ему место: это настройка профиля, а не то, что агент
узнал о мире игрока.

### Кто выбирает режим

Переключатель **«Профиль в запросе»** у каждого агента:

- **`авто`** — режим на каждое сообщение выбирает **роутер**: служебный
  вызов модели перед ответом, на входе — режимы с описанием «когда», режим
  прошлого хода, последний обмен и новое сообщение, на выходе — одно
  название. Ключевые слова здесь не работают: «А если у меня дальняя
  атака?» после вопроса о бое — это продолжение «За столом», а «А что
  значат цифры на жетонах?» после объяснения новичку — продолжение «Учу
  новичка», хотя по словам оба вопроса игровые;
- **режим** — в запрос всегда уходит он, роутер не вызывается;
- **`выключен`** — профиль в запрос не уходит, роутер не вызывается,
  профиль при этом не стирается.

Роутер не вызывается, если выбирать не из чего (меньше двух режимов) или не
нужно (режим выбран вручную или выключен, вопрос пуст). Сбой роутера, пустой
или неразобранный ответ дают запасной режим — режим прошлого хода, если он
ещё есть в профиле, иначе первый режим списка; ход это не отменяет, как и
любой другой служебный вызов.

### Приоритеты в запросе

1. **Системный промпт пресета** — роль и технический формат ответа: JSON у
   пресета «Строгий JSON» профиль отменить не может.
2. **Текущий разговор** — «давай подробнее» важнее формата режима «За
   столом».
3. **Профиль** — явные настройки пользователя.
4. **Долговременная память** — выученное из прошлых разговоров.

Порядок блоков в запросе: системный промпт → **профиль** → долговременная
память → рабочая память → память стратегии → хвост истории → новый вопрос.

### Редактор — форма, а не вид

Интерфейс всюду остаётся видом на состояние агента, кроме одного места:
редактор профиля — это форма ввода, и её поля не входят в общий вид на
состояние агента. Иначе отправка вопроса или смена стратегии затирали бы
несохранённую правку. «Сохранить профиль» разбирает форму, проверяет
(в профиле должен быть хотя бы один режим, у каждого — непустое название,
без повторов и зарезервированных слов `авто`/`выключен`; при двух режимах и
больше — непустое «когда» у каждого) и пишет файл, только если что-то
изменилось. Файл общий для всех агентов и сессий процесса.

### Файл профиля на диске

```json
{
  "version": 1,
  "updated_at": "2026-09-16T22:58:38",
  "profile": {
    "address": "Саша, на «ты»",
    "language": "по-русски; названия героев — по-английски, как на коробке",
    "level": "играю больше года, базовые правила знаю — азы не объясняй",
    "constraints": "не предлагай хоум-рулы, пока я сам не попросил",
    "modes": [
      {"name": "За столом", "when": "…", "style": "…", "format": "…", "constraints": "…", "steps": ["…", "…"]}
    ]
  }
}
```

Нет файла — действует профиль по умолчанию из `presets.py`; после первого
сохранения источник — файл, и правка кода в него уже не доезжает (в отличие
от пресетов, где на диск едет только имя): профиль — данные пользователя, а
не промпт разработчика. Битый файл не затирается: профиль по умолчанию,
запись выключена, причина в логе и в панели.

### Что осталось от дня 11

Ключ `формат ответов` удалён из карты памяти — как отвечать теперь профиль,
а не то, что агент выучил; раздел долговременной памяти `профиль` у
`коллекция`/`игровая группа` переименован в `окружение`, чтобы не путаться с
новым блоком «Профиль пользователя» в запросе. В промпт разбора памяти
добавлено правило: просьбы о том, как отвечать (длина, формат, тон,
обращение, язык), — не запись памяти ни в какой ключ. Старая запись `формат
ответов` в уже существующем `long_term.json` не мигрирует и не удаляется —
она показывается в панели как «в файле, не используется» и не уходит ни в
запрос, ни во вход разбора; перенести её текст в профиль можно вручную, в
редакторе.

## Состояние задачи: конечный автомат (день 13)

Рабочая память (день 11) помнит **что** известно о партии; состояние задачи
помнит **где мы в работе** и чего ждём: этап, текущий шаг, ожидаемое
действие, план с отметками и журнал переходов. Ни одно в другое не пишется.

### Этапы, пауза и таблица переходов

Основной путь — `планирование → выполнение → проверка → готово`; вне него —
`отменена` (только по кнопке человека). **Пауза — не этап, а флаг** на любом
из трёх активных этапов.

| Этап | Ожидается от пользователя | Что делает ассистент |
| --- | --- | --- |
| планирование | утвердить план или сказать, что поправить | предлагает план — шаги и отдельно проверки |
| выполнение | сделать шаг N и сказать, что готово | объясняет только текущий шаг |
| проверка | подтвердить проверку N | проводит текущую проверку |
| готово / отменена | ничего | один раз подводит итог / подтверждает отмену |

Всё допустимое — таблица `task_state.TRANSITIONS`: событие, кто может его
вызвать (человек и/или трекер), из каких этапов, при каком флаге паузы, куда
ведёт переход и условие. Переход, которого в таблице нет для текущего
этапа/паузы/источника, — **отклоняется** с причиной, видной в логе, в панели
и — если отказал трекер — в блоке следующего запроса, чтобы ассистент
объяснил его пользователю. Этап и номер шага модель не пишет никогда —
только код, по таблице.

### Кто двигает автомат

- **Человек — кнопками**: «Начать задачу с этим сообщением», «⏸ Пауза»,
  «▶ Продолжить», «Отменить задачу»; с дня 15 — ещё «↩ Вернуться к плану» и
  «↩ Переоткрыть задачу».
- **Трекер — служебный вызов перед ответом**, четвёртый вокруг того же
  `_call_service_model()`, что свёртка/факты, разбор памяти и роутер
  профиля. На вход — состояние задачи и события с пометкой «можно сейчас» /
  «сейчас нельзя» (до дня 15 — только допустимые), на выход — одно событие
  (или «нет») и, с дня 15, необязательный сход с маршрута. Не вызывается без задачи, на
  конечном этапе, на ходе «Начать задачу» и повторно на месте истории, где
  его переход уже применён.
- **Основная модель — никогда**: видит состояние в блоке запроса и ведёт
  разговор по нему, но переходов не объявляет.

Сбой трекера, как и любого служебного вызова, ход не отменяет. Исключение —
**обрезанный ответ не применяется вовсе** (в отличие от сводки, фактов и
разбора памяти): оборванный ответ трекера — это оборванный план, а
утверждённый план с потерянными шагами пользователь не заметит до этапа
проверки.

### Блок в запросе

Порядок блоков: системный промпт → профиль → долговременная память →
рабочая память → **состояние задачи** → память стратегии → хвост истории →
новый вопрос. На активном этапе (в том числе на паузе) блок уходит всегда;
на «готово»/«отменена» — только ходом сразу после перехода, дальше
завершённая задача в запрос не уходит, но остаётся в панели и в файле.
Переключатель «Состояние задачи в запросе» меняет это; трекер работает при
любом его положении.

### Где лежит и сколько живёт

Состояние и журнал переходов — в `context.task` файла сессии, тем же
приёмом, что рабочая память дня 11: переживают перезапуск, копируются в
ветку из снимка checkpoint'а, очищаются «Сбросить диалог». Переключатель
блока, последний отказ и расход трекера на диск не едут — это инструмент
проверки и бухгалтерия, а не диалог. Одна задача на сессию: начать новую
можно только когда задачи нет или она на этапе «готово»/«отменена».

Проверка паузы на каждом активном этапе и продолжения с блоком состояния и
без него (в ветке с выключенным переключателем, на «Скользящем окне» —
намеренно тесном, чтобы цель и план выпадали из окна) — в
[`docs/TooManyRules — День 13 проверка состояния задачи.md`](docs/TooManyRules%20—%20День%2013%20проверка%20состояния%20задачи.md).
На живом прогоне блок без окна восстанавливает номер шага по общим знаниям,
но путает его содержание и пересказывает шаги, которых просили не
пересказывать, — там же цитаты.

## Инварианты: не память, не профиль и не системный промпт (день 14)

Инвариант — правило, которое ассистент не вправе нарушить. Название карты за
столом ищут по английскому названию на коробке; совет «возьми Ghillie из
Undertow» бесполезен, если дополнений нет; выдуманное правило хуже отсутствия
ответа. Без инвариантов такие рамки держались на системном промпте и удаче:
попроси «переводи названия, жене так понятнее» — и ассистент начнёт переводить.

### Три места и две области

| Область / привязка | Где лежит | Пример |
| --- | --- | --- |
| `всегда` | `src/data/invariants/invariants.json` — общий для всех сессий и агентов | «Английские названия писать как в коробке: не переводить и не транслитерировать» |
| `сессия` | `context.invariants` файла сессии | «В этой партии — только правила: тактических советов не давать» |
| над переходом | там же + событие автомата и режим | «Проверку отмечает пройденной сам игрок» — «проверка пройдена» только с подтверждением |

Номера `П1, П2, …` и `С1, С2, …` выдаются при сборке книги и ключом хранения не
являются. Выключенный инвариант остаётся в файле, но не действует: ни в
блок, ни стражу, ни в автомат. Пока файла нет, действуют инварианты по
умолчанию из `presets.py` (три действуют, два — над переходами — выключены);
после первого сохранения правка кода до файла уже не доезжает.

### Что делает инвариант

1. **Блок в каждом запросе.** Первым из блоков агента, сразу за системным
   промптом (порядок: системный промпт → инварианты → профиль → долговременная →
   рабочая → задача → память стратегии → хвост истории → вопрос). Заголовок
   блока говорит прямо: инварианты старше профиля, памяти, состояния задачи и
   разговора, просьба их снять — повод отказать, а отказывая — назвать
   ограничение своими словами и предложить то, что оно допускает.
2. **Страж — служебный вызов перед ответом.** Во вход идут только действующие
   инварианты без события автомата, последний обмен и новое сообщение;
   ответ — `вердикт: конфликт` с номерами и одной фразой «чем» или `вердикт:
   нет конфликта`. Сомневается — «нет конфликта»: ложный отказ ломает
   нормальный ответ. Конфликт без номеров из списка стража и номера вне списка
   не применяются, но видны в панели и в логе. Страж не вызывается, когда
   сверять нечего: пустая книга, все выключены, остались только инварианты
   над переходами, вопрос пустой. Сбой, пустой и неразобранный ответ ход не
   отменяют; обрезанный потолком применяется с пометкой — в отличие от
   трекера, у стража вердикт стоит первой строкой.
3. **Проверка кодом на переходах.** `запрещён` — переход не состоится ни от
   трекера, ни от кнопки человека; подтверждением запрет не обходится, снять
   его можно только флажком «Действует». `с подтверждением` — событие трекера
   не применяется, а становится ожиданием (состояние не меняется, отказа нет),
   и переход происходит по кнопке «✅ Подтвердить переход»; в журнале он
   остаётся переходом трекера с пометкой «подтверждено пользователем».
   Ожидание на диск не едет.

Ответ модели код не читает и не проверяет: ни вторым вызовом, ни регулярками.
Кодом закрыты только переходы; в остальном день даёт видимость (конфликт
виден независимо от того, послушалась ли модель) и рамку, которую не
вытесняет окно контекста.

### Где лежит и сколько живёт

Постоянные — в своём файле (`JsonInvariantStore`, копия `JsonProfileStore`:
один файл на каталог данных, битый файл не затирается, запись выключается).
Сессионные — в файле сессии, копия в checkpoint'е и в ветке из снимка, «Сбросить
диалог» их чистит, постоянные не трогает. Последний конфликт, ожидание
подтверждения, переключатель «Инварианты в запросе» и расход стража на диск не
едут.

Проверка на живом сценарии — прямой конфликт, давление («забудь про
ограничения»), сессионный инвариант при пустом окне, ветка с выключенным
блоком и оба режима над переходом — в
[`docs/TooManyRules — День 14 проверка инвариантов.md`](docs/TooManyRules%20—%20День%2014%20проверка%20инвариантов.md).
На прогоне блок держал отказы и там, где разговора об ограничении уже нет в
окне; в ветке без блока ассистент согласился переводить названия, хотя страж
конфликт назвал, — цитаты и цифры там же.

## Жёсткий контроль переходов: красный путь (день 15)

День 13 построил маршрут и прошёл его прямо; день 15 проверяет, что с него
нельзя сойти незаметно. Три уровня, от самого твёрдого к самому мягкому:

1. **Состояние не сдвинуть ничем, кроме допустимого перехода.** Это свойство
   кода: `TaskState` меняется только в `_transition()`, а туда попадают,
   пройдя таблицу, ограничения переходов дня 14 и условия. Никакая
   формулировка пользователя, ответ модели или кнопка этого не обходят.
2. **Попытка сдвинуть не туда видна.** Отказ по таблице, сход с маршрута и
   отказ по инварианту идут одним путём: причина → блок запроса этого хода →
   статус хода → панель → журнал ходов (колонки «отказ» и «сход») → лог.
3. **Ответ держит модель.** Код не читает ответ ассистента: блок говорит,
   чего нельзя, а насколько это держится, показывает документ проверки.

### Что изменилось в трекере

Во вход трекера идёт список **всех** его событий с пометкой «МОЖНО СЕЙЧАС» /
«сейчас нельзя (причина)» — пометка выводится из той же функции, что
`allowed()` и причины отказов. Промпт просит называть то, чего просит
пользователь, даже если событие помечено «сейчас нельзя»: допустимость решает
код. Правило «сомневаешься — `нет`» осталось, но теперь оно про то, **есть ли
в сообщении событие**, а не про то, допустимо ли оно. К ответу добавлена
необязательная пара строк `сход` / `чем`.

### Десять переходов

К восьми переходам дня 13 добавлены «вернуться к плану» (трекер и человек, с
выполнения и проверки, не на паузе → планирование) и «переоткрыть» (только
человек, с «готово» → проверка 1). У каждой строки таблицы есть
машиночитаемое `to_stages`, и при импорте `task_state.py` проверяются пять
свойств маршрута (`_validate_route()`): вперёд — не больше чем на один этап,
назад — на любой, все активные этапы достижимы от «начать», тупиков нет,
конечные этапы конечны (из «готово» ведут только «начать» и «переоткрыть», из
«отменена» — только «начать»). Пауза и продолжение помечены `same_stage` —
«строка не меняет этап»; это объявление, а не догадка по совпадению полей.
А поскольку проверка при импорте видит только таблицу, `to_stages` сверяется
ещё раз в момент перехода: если ветка кода насчитала этап, которого у события
в таблице нет, перехода не будет — будет отказ с причиной. «Перепрыгнуть этап
нельзя» — свойство таблицы **и** кода, а не их согласия между собой.

### Блок в запросе: две новые строки

`Сейчас нельзя:` — всегда, пока блок уходит; текст выводит код из этапа и
флага паузы плюс общая строка «этап и шаг меняет приложение; отменить, начать
новую и переоткрыть можно только кнопкой». `Сход с маршрута:` — только на
ходе со сходом. Порядок блоков в запросе и заголовок блока (метка для счёта
токенов) не изменились.

### Где лежит и сколько живёт

Ничего нового на диск не едет: записи откатов — обычные строки журнала
переходов в `context.task`, формат файла сессии — версия 2, миграций нет.
Последний сход, последний отказ и счётчики красного пути — память процесса.
Счётчики считают сообщения, а не попытки: повтор упавшего хода (основной вызов
не удался, история не выросла, то же сообщение отправлено снова) не удваивает
ни «отклонено переходов», ни «сходов с маршрута».

Прогон красного пути показал и то, что стоит знать: трекер на «давай без
плана» и на давление на паузе отметил сход, а не событие, — поэтому отказа
автомата на них нет, но состояние не сдвинулось; вход трекера подорожал
заметно больше, чем ожидала спецификация (замеры — в документе проверки и в
комментарии у `TASK_MAX_TOKENS`).

## MCP: сервер BoardGameGeek (день 16)

По общей спецификации первый внешний источник проекта — BoardGameGeek:
метаданные игр и обсуждения правил в форумах. Для него есть готовый открытый
MCP-сервер **bgg-mcp** (`github.com/kkjdaniel/bgg-mcp`, MIT, Go; в официальном
реестре MCP — `io.github.kkjdaniel/bgg-mcp`, последняя запись 1.6.1). У него
десять инструментов: `bgg-collection`, `bgg-details`, `bgg-hot`, `bgg-price`,
`bgg-recommender`, `bgg-rules`, `bgg-search`, `bgg-thread-details`,
`bgg-trade-finder`, `bgg-user`. Проекту ближе всего `bgg-rules` (ответы на
вопросы по правилам из форумов BGG) и `bgg-thread-details` (тред целиком).
Сегодня приложение только узнаёт, что сервер умеет; вызывать инструменты
ассистент начнёт не сегодня.

Клиент — официальный Python SDK **`mcp` второй мажорной версии**
(`mcp>=2.2,<3` в `requirements.txt`). Его API отличается от v1: большая часть
примеров в интернете (`ClientSession` + `stdio_client` +
`session.initialize()`) — это v1. SDK импортирует только `src/mcp_client.py`,
как LLM API вызывает только `agent.py`.

### Четыре шага соединения

Каждое нажатие кнопки — одно полное подключение, между нажатиями ничего не
хранится:

1. **Запуск процесса.** stdin и stdout сервера становятся каналом JSON-RPC,
   stderr идёт в терминал приложения как есть.
2. **Согласование протокола.** Клиент пробует `server/discover`, сервер
   отвечает «нет такого метода», клиент откатывается к рукопожатию
   `initialize` — и получает имя и версию сервера, версию протокола и его
   возможности. Это и есть «соединение установлено».
3. **`tools/list`** — по всем страницам, пока сервер присылает `nextCursor`.
   Каждая страница разобрана SDK по схеме протокола: битый ответ до панели не
   дойдёт — будет сбой стадии.
4. **Закрытие**: stdin закрыт → сервер выходит; SDK ждёт до 2 с, потом
   завершает процесс сам.

Один потолок времени — `MCP_TIMEOUT_S` в `presets.py` (10 с) — охватывает все
четыре шага. Сбой любого шага — не исключение, а строка статуса со стадией
(`запуск`, `соединение`, `список инструментов`) и строка лога.

### Две эры протокола

Ревизия MCP 2026-07-28 убрала рукопожатие: вместо `initialize` клиент шлёт
один запрос `server/discover`. SDK v2 умеет обе эры и по умолчанию сам
пробует новую, откатываясь к старой. bgg-mcp (библиотека mcp-go 0.43.2) —
сервер старой эры, поэтому соединение с ним всегда выглядит как «проба
`server/discover` не принята — рукопожатие `initialize`, протокол
2025-06-18». Это штатный путь, а не сбой; режим согласования не закреплён,
чтобы тот же клиент подключился и к серверу новой эры.

### Окружение сервера и ключ BGG

- **Процесс сервера не наследует окружение приложения.** SDK передаёт ему
  только `HOME`, `LOGNAME`, `PATH`, `SHELL`, `TERM`, `USER` и то, что клиент
  перечислил явно: `BGG_API_KEY` и `BGG_USERNAME`, если заданы. Ключ DeepSeek
  до чужого сервера не доходит.
- **Значения переменных нигде не показываются и не логируются** — только
  «задан / не задан». Командная строка логируется как есть, поэтому ключей в
  ней не бывает.
- **Для списка инструментов ключ не нужен.** Для вызова инструментов
  понадобится: BGG с 2025 года пускает к XML API только зарегистрированные
  приложения и без токена отвечает 401. Ключ получают вручную на
  [boardgamegeek.com/applications](https://boardgamegeek.com/applications), и
  выдают его не сразу — заявку стоит подать заранее.

### Сборка сервера

Сервер запускается **бинарником, собранным из исходников на теге v1.6.1**;
Docker не используется. README сервера рекомендует Docker, но бинарник
поднимается за десятки миллисекунд, не требует запущенного рантайма и
скачанного образа, а Go ставится за минуту. Цена — изоляции нет: сервер
работает процессом с правами пользователя. Окружение приложения (ключ
DeepSeek) он при этом не получает — его отсекает белый список SDK.

Нужен Go ≥ 1.23 (после сборки он не нужен). Собирать — только из клона на
теге: `go install github.com/kkjdaniel/bgg-mcp@latest` не работает, во всех
тегах в `go.mod` опечатка в пути модуля.

```bash
git clone https://github.com/kkjdaniel/bgg-mcp.git
```

```bash
cd bgg-mcp && git checkout v1.6.1 && go build -o "$HOME/.local/bin/bgg-mcp"
```

Командная строка по умолчанию в `presets.py` — `~/.local/bin/bgg-mcp -mode
stdio`: путь строится от домашнего каталога текущего пользователя, поэтому
после сборки в `src/.env` ничего не нужно. Если бинарник лежит в другом месте,
переменная `TOOMANYRULES_BGG_MCP_COMMAND` в `src/.env` заменяет командную
строку **целиком**; путь — абсолютный (тильда и переменные в строке не
раскрываются):

```
TOOMANYRULES_BGG_MCP_COMMAND=/абсолютный/путь/bgg-mcp -mode stdio
```

**`src/.env`** — все новые строки необязательны:

```
DEEPSEEK_API_KEY=...
# День 16, MCP BoardGameGeek — всё необязательно:
# BGG_API_KEY=...          ключ XML API BGG (boardgamegeek.com/applications); для списка инструментов не нужен
# BGG_USERNAME=...         ник на BGG — для «моя коллекция» в инструментах сервера
# TOOMANYRULES_BGG_MCP_COMMAND=/абсолютный/путь/bgg-mcp -mode stdio    — только если бинарник не в ~/.local/bin
```

Правка `.env` доезжает после перезапуска приложения. Для разовой проверки
переменную удобнее задать в команде запуска — `load_dotenv()` уже заданное
не перезаписывает:

```bash
TOOMANYRULES_BGG_MCP_COMMAND="sleep 100" ./run.sh
```

Приложение поднимается и работает и без собранного сервера: подключения при
старте нет (только строка лога «MCP при старте» — чем будет запущен сервер), а
сбой подключения — строка статуса, чат и всё остальное от блока MCP не зависят.

### Необязательная сверка: MCP Inspector

Независимый официальный клиент — чтобы показать, что список «корректный» не
только в нашей панели. Нужен Node ≥ 22.19. Всё до `--` — команда сервера
вместе с её флагами, всё после — параметры самого Inspector:

```bash
npx -y @modelcontextprotocol/inspector --cli "$HOME/.local/bin/bgg-mcp" -mode stdio -- --method tools/list
```

```bash
npx -y @modelcontextprotocol/inspector --cli "$HOME/.local/bin/bgg-mcp" -mode stdio -- --method initialize
```

### Если не подключается

| Стадия и текст | Причина | Что сделать |
| --- | --- | --- |
| «запуск», `No such file or directory: '/…/.local/bin/bgg-mcp'` | бинарник не собран или лежит в другом месте | собрать (см. выше); или указать абсолютный путь в `TOOMANYRULES_BGG_MCP_COMMAND` |
| «запуск», `No such file or directory: '~/…'` | в замене путь с тильдой | написать путь абсолютным |
| «соединение», `Connection closed`, в терминале сообщение сервера | сервер не понял флаги (например, «Invalid mode: …») или упал на старте | прочитать его сообщение в терминале над строкой лога; проверить `-mode stdio` в замене |
| «соединение», таймаут | в замене не MCP-сервер, или сервер завис | проверить командную строку в строке лога «MCP при старте» |
| после нажатий `pgrep -fl bgg-mcp` не пуст | процесс не закрылся | так быть не должно — записать и разобраться |

### Где лежит и сколько живёт

Ничего нового на диск не едет. Описание сервера и потолок времени — код
(`presets.py`); замена командной строки, ключ и ник — `src/.env`; результат
подключения — выходы блока в этой вкладке, до перезагрузки страницы; процесс
сервера — одно нажатие. Ни в файле сессии, ни в агенте, ни в checkpoint'е и
ветке, ни в общем виде панели (`_view()`) результата подключения нет: к диалогу
MCP сегодня не относится вовсе.

Подключение к MCP — не вызов модели: счётчики агента и процесса не
двигаются. Одно число статус показывает на будущее: сколько токенов заняли бы
описания и схемы всех инструментов (по оценке `tokens.py`, у bgg-mcp —
≈1 800), если отдать их модели: каждый MCP-инструмент добавляет свои токены к
**каждому** запросу.

## Свой MCP-сервер FAQ (день 17)

Источник — официальный FAQ издателя по Too Many Bones:
[support.chiptheorygames.com/support/solutions/33000133540](https://support.chiptheorygames.com/support/solutions/33000133540).
Это разъяснения Chip Theory Games как раз там, где модель ошибается чаще всего:
порядок эффектов начала хода, Poison, Engulf и Break, боты Tink, отдельные
столкновения, лут — 82 вопроса в 24 разделах. Открытого API у портала нет (API
Freshdesk требует ключ агента поддержки), поэтому сервер читает страницы сайта
тем же HTTP GET, что и браузер: у категорий, разделов и статей стабильные
адреса и разметка.

Сервер — **отдельная программа, а не модуль приложения**: `mcp_client`
запускает его процессом (`python src/faq_server.py --base-url … --category …
--category-name …`), приложение его не импортирует, и он не импортирует ничего
из проекта. Клиентскую часть SDK `mcp` импортирует только `mcp_client.py`,
серверную — только `faq_server.py`. Про Too Many Bones сервер не знает: адрес
портала, id категории и её имя приходят аргументами из `presets.FAQ_MCP`.

### Почему категория, а не весь FAQ

У издателя есть ещё одна игра с Too Many Bones в названии — 20 Strong, и в её
категории лежит **папка** «Too Many Bones»: правило колоды Too Many Bones для
20 Strong, а не для нашей игры. Сервер, собирающий всё с «Too Many Bones» от
корня FAQ, выдал бы её за официальный ответ. Поэтому стартовая точка — id
категории `33000133540` (стабильный ключ Freshdesk), плюс две проверки: имя
категории сверяется с заголовком её страницы (несовпадение — ошибка, а не
чужой FAQ), а `faq_article` отдаёт только статью, у которой в хлебных крошках
наша категория. Номер статьи 20 Strong — ошибка инструмента. Поиск сайта не
используется: `/support/search` закрыт в robots.txt; подходящий вопрос по
списку выбирает модель.

### Два инструмента

| Инструмент | Параметры | Что возвращает |
| --- | --- | --- |
| `faq_questions` | `section` — необязательный фильтр по части английского названия раздела (имя героя, Battle, Loot…) | строки «номер · раздел · вопрос» в порядке сайта; разделы, где статей больше пяти, дочитываются со всех страниц пагинации; промах фильтра — перечень разделов, а не ошибка |
| `faq_article` | `article_id` — номер из `faq_questions`, обязательный | вопрос, раздел, каноническую ссылку и ответ издателя текстом |

У каждого параметра — описание в JSON Schema (`Field(description=…)`); оно
видно в таблице блока FAQ. Аннотации честные: только чтение, повтор безопасен,
мир открытый. Разбор страниц — `html.parser` из стандартной библиотеки,
маркеры разметки — константы в начале файла; HTTP — `httpx`, таймаут 10 с на
запрос, не больше четырёх запросов одновременно, повторов нет. Ожидаемый сбой
(сеть, ответ не 200, 404, не та категория, не разобранная разметка) — `ToolError`
с текстом по-русски: SDK делает из него ответ с `isError: true`, модель видит
текст и может объяснить игроку, что FAQ недоступен. Логи сервера — одной
строкой на запрос к сайту и на вызов, с префиксом `[FAQ-сервер]`, в stderr, то
есть в терминал приложения.

Описания инструментов — это и есть промпт: когда звать инструмент, модель
узнаёт из них, системные промпты пресетов не менялись. Две правки описаний по
живому прогону записаны комментариями у констант в `faq_server.py`.

### Как агент пользуется инструментами

Инструменты — ещё одна необязательная зависимость агента (протокол `ToolBox`
в `agent.py`, реализация — `mcp_client.McpToolBox`, одна на процесс). В `ask()`
после всех служебных работ агент запрашивает каталог (одно подключение к
серверу), и основной запрос хода становится циклом **раундов**:

```
раунд 1   модель: сообщения + tools=[2 схемы]  ← tool_calls: faq_questions(section="Patches")
          mcp_client.call_tool(...)            → 2 строки
раунд 2   модель: + ответ с tool_calls + tool  ← tool_calls: faq_article(article_id=33000210180)
          mcp_client.call_tool(...)            → вопрос, ссылка, ответ
раунд 3   модель: + ответ с tool_calls + tool  ← stop: «Нет — Patches лечит только Gearloc'ов… (ссылка)»
```

- **Решает модель**: вызывать ли инструмент, какой и с какими аргументами. Код
  вопрос не классифицирует и инструменты сам не вызывает.
- **Раунд — настоящий вызов API**: каждый считается в счётчиках агента и
  процесса. Цена и время хода — суммы по раундам, оценка до запроса — только
  первого раунда (калибровка сравнивает её с `prompt_tokens` первого раунда).
- **Имя не из каталога и неразобранные аргументы** на сервер не уходят —
  модели возвращается текст ошибки; результат длиннее `TOOL_RESULT_MAX_CHARS`
  (12 000 символов) обрезается с пометкой.
- **Потолок** — `TOOL_MAX_ROUNDS` (4) раундов с инструментами; если и
  последний закончился вызовами — ещё один, финальный, с `tool_choice="none"`.
- **В историю идут только вопрос и финальный ответ.** Сообщения с `tool_calls`
  и результаты инструментов живут только в пределах хода: формат файла сессии,
  стратегии, разбор памяти, трекер и checkpoint'ы рассчитаны на пары
  «вопрос/ответ». Цена — на следующем ходе модель видит только свой пересказ
  статьи.
- **Сбой каталога** ход не отменяет — запрос уходит без инструментов; сбой
  вызова — текст для модели; сбой API на раунде > 1 — ход неудачен целиком,
  стек не меняется.
- **Пресет с thinking**: внутри хода рассуждение раунда с `tool_calls` уходит
  обратно, как требует документация DeepSeek; между ходами история остаётся
  только текстом — `deepseek-v4-pro` это принимает (проверено, замер у
  `FAQ_MCP`).

### Две эры протокола — теперь с другой стороны

bgg-mcp дня 16 — сервер старой эры: проба `server/discover` не принимается,
рукопожатие `initialize`, протокол 2025-06-18. Свой сервер на SDK v2 понимает
эру 2026-07-28, и тот же клиент в режиме по умолчанию договаривается с ним
**через `server/discover`, без рукопожатия**. В панели обе строки рядом.

### Цена

- **Схемы в каждом раунде**: описания и схемы двух инструментов — ≈500
  токенов по оценке (корзина «схемы инструментов» в бюджете контекста); ход
  без вызовов дороже на неё, чем на дне 16.
- **Лишние раунды**: ход с вызовами — три запроса к модели, каждый длиннее
  предыдущего на результаты инструментов (полный список вопросов — ≈8,7 тыс.
  символов).
- **Время**: каждый вызов — новый процесс сервера (≈0,4-0,5 с) и страницы
  сайта: каталог ≈0,5 с, `faq_article` ≈1,6 с, `faq_questions` с фильтром
  ≈1,7 с, без фильтра ≈4 с. Ход с двумя вызовами дольше обычного на 5-8 с.
- **Служебных вызовов модели это не добавляет**: раунды — части основного
  запроса.

Числа реального прогона (И1-И5, пресет с thinking, сбои) — комментарием у
`FAQ_MCP` в `presets.py`.

### Замена командной строки

`TOOMANYRULES_FAQ_MCP_COMMAND` в `src/.env` (или в команде запуска) заменяет
командную строку сервера **целиком**, как у BGG. Нужна для проверки сбоев и на
случай запуска другим интерпретатором. Недоступный сайт:

```bash
TOOMANYRULES_FAQ_MCP_COMMAND="src/.venv/bin/python src/faq_server.py --base-url https://nonexistent.invalid --category 33000133540 --category-name 'Too Many Bones'" ./run.sh
```

Сбой каталога (например, нет скрипта) — ход идёт без инструментов, как на дне
16; сбой вызова (сайт недоступен, не та категория) — ошибка инструмента уходит
модели, ассистент объясняет, приложение работает дальше. Собирать ничего не
нужно: сервер запускается тем же Python из `src/.venv`, нужен только доступ к
сайту издателя.

### Необязательная сверка: MCP Inspector

```bash
npx -y @modelcontextprotocol/inspector --cli src/.venv/bin/python src/faq_server.py --base-url https://support.chiptheorygames.com --category 33000133540 --category-name "Too Many Bones" -- --method tools/list
```

С `--method tools/call --tool-name faq_article --tool-arg article_id=33000210180`
— текст статьи. Синтаксис — по документации Inspector; на прогоне дня 17 эти
команды не запускались.

### Где лежит и сколько живёт

Ничего нового на диск не едет, формат файла сессии не менялся. Описание
сервера, категория и лимиты — код (`presets.py`); процесс сервера и страницы
сайта — один вызов; каталог — один ход; промежуточные сообщения раундов —
один ход; вызовы последнего хода — ответ агента, как метрики последнего
вызова; положение переключателя — агент (у нового, восстановленного агента и
у ветки — включено); финальный ответ по статье — история, как весь диалог.
Кэша нет ни в сервере, ни в приложении: сайт читается при каждом вызове.

## Сторож FAQ (день 18)

Во всём, чем пользуется ассистент, со временем меняется одно — официальный
FAQ издателя: Chip Theory Games дописывает разъяснения и правит старые ответы
(статья про энкаунтер «Gearlocs Need Food Badly» изменена на сайте
15.09.2026). Игроку полезно узнать о правке, не перечитывая FAQ, поэтому «агент
24/7» дня — **сторож FAQ**: сервер, который работает сам по себе, без
приложения.

1. **По расписанию** (по умолчанию раз в сутки) снимает FAQ той же категории,
   что сервер дня 17, целиком — список вопросов и тексты всех 82 статей (89
   запросов к сайту, 15-17 с) — и сохраняет снимок в SQLite.
2. Сравнивает снимок с прошлым удачным и **после каждого снимка сам пишет
   сводку** в свой терминал: сколько статей, запросов и секунд, события по
   видам и строка на каждое событие, время следующего снимка.
3. По вызову `faq_changes` отдаёт **агрегированную сводку за период** — из
   базы, за миллисекунды.
4. По вызову `faq_watch_schedule` **меняет расписание**: вызов возвращается
   сразу, а снимок по новому расписанию происходит потом, в фоне.

### Как запустить

```bash
./run.sh faq-watch
```

Скрипт поднимает тот же `src/.venv` и запускает `src/faq_watch.py`
командной строкой из `presets.FAQ_WATCH_ARGV`: адрес портала, категория и её
имя — те же, что у сервера дня 17, база — `src/data/faq_watch/faq_watch.sqlite3`
(не в git), адрес — `http://127.0.0.1:8765/mcp`. Аргументы после `faq-watch`
дописываются в конец и перекрывают прежние:

```bash
./run.sh faq-watch --interval 5
```

```bash
./run.sh faq-watch --port 8766 --db /tmp/faq-watch-test/faq_watch.sqlite3
```

- `--interval` — минуты, от 5 до 10080 (неделя). Задан — перекрывает
  сохранённый в базе и сохраняется сам; не задан — из базы или раз в сутки.
  5 минут — режим для записи видео, а не для постоянной работы: полный снимок
  — 89 запросов к чужому сайту.
- `--port` — порт на `127.0.0.1` (наружу сервер не открывается); приложению
  адрес своего экземпляра сообщает `TOOMANYRULES_FAQ_WATCH_URL` в `src/.env`.
- `--db` — путь к базе; каталог создаётся. Чужая или битая база (другая
  версия схемы, не SQLite, база с чужими таблицами) не затирается: сервер
  пишет причину и не стартует. Занятый порт — то же, до первой записи в базу.

Ctrl+C останавливает сервер штатно; снимок, прерванный остановкой, в базу не
попадает. Ключей у сервера нет, `src/.env` ему не нужен и не передаётся:
`run.sh` запускает его с окружением терминала, а не приложения.

### Расписание

- **Кто задал интервал последним, тот и прав**: аргумент запуска или
  инструмент `faq_watch_schedule` — значение хранится в базе и переживает
  перезапуск сервера.
- **Срок считается от истории в базе, а не от старта процесса**: удачных
  снимков нет — снимок сразу; иначе — последний удачный плюс интервал; после
  неудачной попытки — повтор через `min(интервал, 15 мин)`. Если сайт
  прочитан, но база снимок не приняла (заблокирована, диск полон), повтор —
  тоже не раньше чем через `min(интервал, 15 мин)`: сайт не перечитывается
  каждую минуту. Сервер,
  остановленный на ночь, утром снимет FAQ сразу, а перезапущенный через минуту
  подождёт свой срок.
- **Стенные часы**: планировщик спит не дольше минуты и каждый раз сверяет
  время со сроком — ноутбук, который засыпал, снимок не пропустит.
- **Снимки не перекрываются**, смена интервала будит планировщик сразу.
- **Внеочередного снимка нет** отдельной кнопкой: если сократить интервал и
  срок уже прошёл, снимок начинается сразу.

### Снимок и сравнение

Снимок — по статье: номер, раздел, вопрос, дата «Modified on» со страницы
статьи (как текст и разобранная), хеш и длина текста ответа; сами тексты
лежат отдельно, по хешу, без повторов. **Сравнивается только полный снимок**:
если хоть одна страница не прочиталась или в разделе прочитано не столько
статей, сколько объявлено, попытка записывается неудачной с причиной, без
статей — иначе недочитанный раздел выглядел бы как «удалены 13 статей».
Снимок пишется одной транзакцией.

| Событие | Когда |
| --- | --- |
| `новая` | номера статьи в прошлом снимке не было |
| `изменена` | другой вопрос или другой текст; в сводке — что было и что стало, у текста — строки разницы |
| `перенесена` | другой раздел |
| `удалена` | номер был в прошлом снимке, в новом его нет |

Одна смена даты «Modified on» без смены вопроса и текста событием не
считается. У первого снимка сравнивать не с чем — «первый снимок: 82 статьи,
база для сравнения».

### Два инструмента

| Инструмент | Параметры | Что возвращает |
| --- | --- | --- |
| `faq_changes` | `days` — 1-365, по умолчанию 7 | состояние сторожа (расписание, снимки и сбои за период, последний и следующий снимок), изменения, замеченные сторожем (сравнение соседних снимков), и статьи, у которых дата изменения на сайте попадает в период (по последнему снимку). Только база — сайт не читается |
| `faq_watch_schedule` | `interval_minutes` — 5-10080, обязательный | новое расписание и время следующего снимка |

В сводке два источника, и оба подписаны: «замечено сторожем» знает только
то, что случилось после его первого снимка, а «по датам сайта» знает и о
правках до него, но не говорит, что именно поменялось. Часовой пояс портала
на странице не указан, поэтому граница периода по датам сайта — с
точностью до суток. Длину сводки держит сервер: не больше 40 статей в каждой
части, 12 строк разницы на статью и 10 000 символов на весь ответ — меньше
потолка результата в приложении, чтобы часть «по датам сайта» не срезалась.
Событиям, которым не хватило места, разница не показывается, а то, что не
поместилось совсем, считается строкой «… и ещё N».

### Как агент ими пользуется

В приложении `TOOLBOX` — `mcp_client.McpToolBoxGroup` из двух серверов: FAQ
дня 17 по stdio и сторож по HTTP. Каталоги сливаются, вызов уходит серверу,
который объявил инструмент; агент при этом не менялся — группа отвечает тому
же протоколу `ToolBox`. **Сторож не запущен — ход идёт с инструментами
FAQ**, а в заметке каталога хода и в логе агента — ⚠️ «watch: сбой на стадии
«соединение»: сервер не отвечает…». Описания инструментов — промпт: сводку
модель зовёт на вопросы о новостях FAQ, расписание — только по прямой просьбе;
на вопрос о правилах идут инструменты дня 17.

### Чем HTTP-сервер отличается от stdio дня 17

| | Сервер FAQ (день 17) | Сторож FAQ (день 18) |
| --- | --- | --- |
| Транспорт | stdio | Streamable HTTP, `127.0.0.1`, `stateless_http=True` |
| Кто запускает | `mcp_client`, на каждое подключение | человек, `./run.sh faq-watch` |
| Сколько живёт процесс | одно подключение | до остановки; переживает перезапуски приложения |
| Состояние | нет | база SQLite и расписание |
| Окружение | белый список SDK | окружение приложения не передаётся вовсе |
| Сбой «не запущен» | стадия «запуск» | стадия «соединение»: «сервер не отвечает по адресу» |
| Сайт | на каждый вызов | только планировщик |

Подключения клиента остаются короткими — одно на вызов, ≈0,01-0,03 с. Фоновая
задача живёт в `lifespan` сервера: в SDK v2 для Streamable HTTP он входит один
раз на процесс, а не на подключение. Приложение сервер **не опрашивает**:
круглосуточно работает сервер, и сводку сам по себе он пишет в свой терминал;
приложение — окно, в котором можно спросить.

### Где лежит и сколько живёт

| Что | Где | Переживает |
| --- | --- | --- |
| снимки, тексты, события, попытки, интервал | `src/data/faq_watch/faq_watch.sqlite3` | перезапуск сервера и приложения |
| срок следующего снимка | вычисляется из базы | — |
| процесс сервера | запущен человеком | перезапуск приложения |
| подключение клиента | один вызов | — |
| сводка в чате | история диалога | как весь диалог |

Формат файла сессии не менялся; базу открывает только `faq_watch.py`.
Очистки старых снимков нет. Числа реального прогона — комментарием у
`FAQ_WATCH` в `presets.py`.

## Структура кода

| Файл | Назначение |
| --- | --- |
| `src/agent.py` | `AgentConfig`, `AgentReply`, `TurnStats`, `ServiceCall`, `ModelRound`, `ToolCallRecord`, `ContextView`, `Checkpoint`, `BranchOrigin`, `CandidateDecision`, `Agent`, протоколы `HistoryStore`, `LongTermStore`, `ProfileStore`, `InvariantStore` и `ToolBox` (день 17), счётчики процесса и реестр агентов, `branch_family()`/`branch_parent()`. **Единственное место, где вызывается LLM API** — в двух точках: основной вызов (с дня 17 — цикл раундов function calling) и `_call_service_model()`, через который идут служебный вызов стратегии, разбор памяти (день 11), роутер профиля (день 12), трекер задачи (день 13) и страж инвариантов (день 14). С дня 18 каталог `ToolBox` может прийти не целиком — необязательный `warning` дописывается к заметке каталога хода и уходит в лог. Не знает ни про Gradio, ни про Too Many Bones, ни про MCP |
| `src/memory.py` | Модель памяти (день 11): слои, карта памяти (`MemorySlot`), `AgentMemory` — рабочая память сессии, кандидаты в долговременную, сборка входа и раскладка ответа разбора по карте, блоки слоёв для запроса. `describe()`, `prepare()` и сборка блоков чистые, состояние меняют только `apply()`, `take_candidates()`, `drop_candidates()`, `load()`, `reset()`. Ни сети, ни диска, ни логов, ни Gradio, ни Too Many Bones; из проекта импортирует только `context.py` |
| `src/user_profile.py` | Профиль пользователя и выбор режима (день 12): `UserProfile`, `ProfileMode`, `ModeChoice`, `RouteTask`, разбор/проверка/запись профиля, блок и текст профиля, `ModeRouter` — положение переключателя, режим прошлого хода, вход роутера и разбор его ответа. Чистые методы (`choices()`, `preview()`, `prepare()`, сборка блока) зовёт панель на каждый рендер, состояние меняют только `set_choice()`, `settle()`, `reset()`. Ни сети, ни диска, ни логов, ни Gradio, ни Too Many Bones; из проекта импортирует только `context.py` (нормализация ключей) |
| `src/task_state.py` | Состояние задачи — конечный автомат (день 13): этапы, события, источники, таблица `TRANSITIONS`, `TaskState`, `TransitionRecord`, `Rejection`, `Outcome`, `TrackerTask`, `TaskView`, `TaskMachine` — состояние, вход трекера, применение событий по таблице, блок для запроса. День 14 добавляет ограничения переходов (`TransitionGuard`, режимы `запрещён`/`с подтверждением`), ожидание подтверждения (`Pending`) и `confirm()` — только добавлениями. День 15 — две строки таблицы (откат назад по графу), `to_stages` у каждой строки, проверку свойств маршрута при импорте (`_validate_route()`), сход с маршрута (`OffRoute`, виды `пропустить этап`/`игнорировать порядок`) и строку «Сейчас нельзя» в блоке. `allowed()`, `describe()`, `block()`, `prepare()` чистые, состояние меняют только `start()`, `fire()`, `apply()`, `confirm()`, `load()`, `reset()`. Ни сети, ни диска, ни логов, ни чтения часов (время — параметром `at`), ни Gradio, ни Too Many Bones; из проекта импортирует только `context.py` (`parse_changes()`, `normalize_key()`) |
| `src/invariants.py` | Инварианты (день 14): области, `Invariant`, `Conflict`, `GuardTask`, `InvariantView`, `InvariantBook` — сессионные инварианты, книга с номерами, блок запроса, вход стража и разбор его ответа, конфликт; `parse_invariants()`, `validate()`, `dump_invariants()`. `items()`, `describe()`, `block()`, `prepare()`, `transition_invariants()` чистые, состояние меняют только `apply()`, `set_session()`, `load()`, `reset()`. Ни сети, ни диска, ни логов, ни чтения часов, ни Gradio, ни Too Many Bones; из проекта импортирует только `context.py`, про `task_state.py` не знает |
| `src/tokens.py` | Оценка токенов без обращения к API, разложение запроса по частям (корзины долговременной и рабочей памяти с дня 11, профиля — с дня 12, задачи — с дня 13, инвариантов — с дня 14, схем инструментов — с дня 17), таблица контекстных окон, бюджет контекста и генератор заполнителя. Чистые функции: ни сети, ни состояния, ни импортов из проекта |
| `src/context.py` | Стратегии управления контекстом: что из истории уходит в модель. Протокол `ContextStrategy`, «Вся история», «Сводка + последние N», «Скользящее окно», «Факты + последние N» и общий разбор строк «ключ: значение» `parse_changes()` (вынесен из фактов на дне 11, дальше его же зовёт разбор трекера дня 13). `describe()`, `prepare()` и `build()` чистые — их зовёт панель на каждый рендер; память меняет только `apply()`, который сообщает, применился ли результат. Ни сети, ни Gradio, ни Too Many Bones, ни импортов из проекта; дни 12-13 код не меняют |
| `src/presets.py` | Системный промпт проекта, набор конфигов агентов, промпты свёртки и фактов, параметры всех стратегий, реестр стратегий и сценарий сравнения (день 10); карта памяти `MEMORY_MAP`, промпт разбора памяти, его параметры, `make_memory()` и сценарий проверки слоёв (день 11); профиль по умолчанию `DEFAULT_PROFILE`, промпт роутера, его параметры, `make_router()` и сценарий проверки персонализации (день 12); промпт трекера, его параметры, `make_task_machine()` и сценарий проверки состояния задачи (день 13; с дня 15 промпт трекера видит все события и отмечает сход с маршрута, добавлен сценарий проверки переходов `ROUTE_SCENARIO`); инварианты по умолчанию `DEFAULT_INVARIANTS`, промпт стража, его параметры, `make_invariants()`, заготовки редактора и сценарий проверки инвариантов (день 14); описание MCP-сервера BoardGameGeek `BGG_MCP`, потолок `MCP_TIMEOUT_S` и замер реального подключения (день 16); описание своего сервера FAQ `FAQ_MCP` (категория, имя, интерпретатор venv), `FAQ_TIMEOUT_S`, лимиты цикла раундов `TOOL_MAX_ROUNDS` и `TOOL_RESULT_MAX_CHARS`, сценарий `TOOLS_SCENARIO` и замер реального прогона (день 17); сторож FAQ — `FAQ_WATCH` (адрес, замена `TOOMANYRULES_FAQ_WATCH_URL`), командная строка `FAQ_WATCH_ARGV` для `./run.sh faq-watch`, `FAQ_WATCH_DB`, `FAQ_WATCH_PORT`, `WATCH_TIMEOUT_S`, сценарий `WATCH_SCENARIO` и замер реального прогона (день 18) |
| `src/storage.py` | `JsonHistoryStore` — реализация протокола `HistoryStore` на JSON-файлах плюс каталог сессий. Checkpoint'ы, происхождение ветки, рабочая память и состояние задачи едут в тот же непрозрачный блок `context`, который хранилище не разбирает; формат файла и версия (2) не меняются. `JsonLongTermStore` (день 11) — долговременная память в отдельном файле рядом с каталогом сессий. `JsonProfileStore` (день 12) — профиль пользователя тем же приёмом, в своём файле. `JsonInvariantStore` (день 14) — постоянные инварианты, копия профильного хранилища, в `src/data/invariants/invariants.json`. Все три хранилища — один экземпляр на процесс, общий для всех агентов; сессионные инварианты едут в непрозрачный `context` файла сессии. Не знает ни про Gradio, ни про Too Many Bones, ни про карту памяти, ни про поля профиля, ни про автомат задачи, ни про инварианты |
| `src/mcp_client.py` | MCP-клиент (день 16): `McpServer`, `Launch`, `McpParam`, `McpTool`, `ToolListing`; `resolve_launch()` — чем будет запущен сервер при данном окружении (чистая), `tool_params()` — параметры инструмента из JSON Schema (чистая), `list_tools()` — одно полное подключение: запуск stdio-сервера, согласование, `tools/list` по всем страницам, закрытие. День 17 — `call_tool()` (одно подключение на один `tools/call`, результат `ToolCall` со стадией, `error` и `is_error`) и адаптер `McpToolBox` — каталог и вызов словарями для агента. День 18 — второй транспорт: сервер по адресу (`McpServer.url`/`url_env`, `Launch.transport`, проверка адреса; Streamable HTTP; «сервер не отвечает» — стадия «соединение» без трассировки) и `McpToolBoxGroup` — несколько серверов как один `ToolBox` (каталоги сливаются, сбой одного — `warning`, вызов — серверу инструмента). **Единственное место, где импортируется клиентская часть SDK `mcp`.** Публичный API синхронный (`anyio.run()` внутри), состояния между вызовами нет, на сбоях исключений не бросает — возвращает результат со стадией. Лист графа: из проекта не импортирует ничего; ни Gradio, ни Too Many Bones, ни BoardGameGeek, ни LLM — описание сервера приходит параметром из `presets.py` |
| `src/faq_server.py` | Свой MCP-сервер (день 17) — **отдельная программа, а не модуль приложения**: stdio, SDK v2 `MCPServer`, инструменты `faq_questions` и `faq_article` вокруг одной категории FAQ портала Freshdesk; адрес, id категории и её имя — аргументами командной строки. Разбор страниц — `html.parser`, HTTP — `httpx`; маркеры разметки — константы; ошибки — `ToolError`; логи — в stderr. С дня 18 чтение сайта — публичные `read_sections()`, `read_article()` и `Article` (с датой «Modified on»): их берёт сторож; ответы инструментов не изменились. Серверную часть SDK `mcp` импортирует он и `faq_watch.py`. Вне графа импортов: приложение запускает его процессом |
| `src/faq_watch.py` | Сторож FAQ (день 18) — **отдельная долгоживущая программа**: Streamable HTTP на `127.0.0.1`, SDK v2 `MCPServer` со `stateless_http=True`, планировщик снимков в `lifespan`, база SQLite (единственное место работы с ней), сравнение снимков, инструменты `faq_changes` и `faq_watch_schedule`; логи — в stderr с префиксом `[FAQ-сторож]`. Импортирует `faq_server.py` (чтение сайта) — ребро между двумя серверами вне графа приложения. Запускает человек (`./run.sh faq-watch`), приложение его не импортирует; про Too Many Bones не знает |
| `src/app.py` | Только интерфейс на Gradio: чат + дебаг-панель, переключатели пресета/стратегии/ветки/слоёв памяти/инвариантов/профиля/состояния задачи, редакторы профиля и инвариантов (единственные формы в интерфейсе — их поля не входят в общий вид на состояние агента), checkpoint'ы, ряд кандидатов в долговременную память, ряд кнопок задачи с кнопкой подтверждения перехода, блоки «Инварианты», «Профиль», «Слои памяти» и «Состояние задачи» с таблицей автомата в закрытом аккордеоне, таблица «Агенты процесса», с дня 16 — блок MCP наверху дебаг-панели (свои выходы, вне общего вида на состояние агента), с дня 17 — переключатель «Инструменты MCP в запросе» и блок своего сервера FAQ: каталог по кнопке (функции дня 16, параметризованные сервером) и «Инструменты последнего хода» (в общем виде — 37 значений), с дня 18 — группа инструментов `McpToolBoxGroup` у агентов и блок «Сторож FAQ»: описание сервера по адресу, каталог и сводка `faq_changes` по кнопке (вне общего вида), «Инструменты последнего хода» переехали сюда; LLM-логики не содержит |
| `src/app_week1.py` | Замороженный артефакт недели 1 (дни 1-5), вне графа импортов нового кода |

Направление зависимостей одностороннее: `app.py` → `presets.py` → `agent.py`
→ `tokens.py`, `context.py`, `memory.py` → `context.py`, `user_profile.py`
→ `context.py`, `task_state.py` → `context.py` и `invariants.py` →
`context.py`; `presets.py` → `memory.py`, `user_profile.py`, `task_state.py` и
`invariants.py`; плюс `app.py` →
`storage.py`, `app.py` → `tokens.py` (оттуда интерфейс берёт только
генератор заполнителя и оценку его размера), `app.py` → `memory.py` (имена
слоёв и текст долговременной памяти для панели), `app.py` → `user_profile.py`
(разбор, проверка и текст профиля для редактора и панели) и `app.py` →
`task_state.py` (константы этапов/событий и таблица переходов для панели) и
`app.py` → `invariants.py` (разбор, проверка и запись инвариантов для
редактора). С дня 16 — `presets.py` → `mcp_client.py` (тип `McpServer`) и
`app.py` → `mcp_client.py` (подключение, описание запуска, версия SDK и белый
список окружения); `mcp_client.py` — лист графа. С дня 17 `app.py` берёт из
`mcp_client.py` ещё и `McpToolBox`; `agent.py` его не импортирует — `ToolBox`
протокол; `faq_server.py` вне графа (запускается процессом). С дня 18 —
ещё `McpToolBoxGroup`; `faq_watch.py` вне графа приложения (его запускает
человек), а его ребро к `faq_server.py` — между двумя серверами. Из проекта `agent.py` импортирует только листья графа и
`memory.py`/`user_profile.py`/`task_state.py`/`invariants.py`, которые все
зависят только от листа `context.py`. Благодаря этому агента можно позже поднять за HTTP или
переключить на локальную модель (`AgentConfig.base_url`), не трогая
интерфейс.

Модель памяти и хранилище долговременной памяти (день 11), профиль и роутер
(день 12), автомат задачи (день 13), книга инвариантов и хранилище
постоянных инвариантов (день 14), инструменты (`ToolBox`, день 17) — восемь
необязательных зависимостей агента, как хранилище истории и стратегии: без
них агент ведёт себя ровно как на дне 10 (без первых двух), на дне 11 (без
следующих двух), на дне 12 (без автомата), на дне 13 (без книги) или на дне
16 (без инструментов). Экземпляр модели памяти, роутера,
автомата и книги свой у каждого агента (`presets.make_memory()`,
`presets.make_router()`, `presets.make_task_machine()`,
`presets.make_invariants()`) — рабочая память, положение переключателей,
задача и сессионные инварианты относятся к конкретному агенту; хранилища
долговременной памяти, профиля и инвариантов и `ToolBox` — по одному на
процесс.

Стратегия подключена к агенту третьей зависимостью после конфига и хранилища
и, как хранилище, необязательна: без неё агент ведёт себя ровно как на дне 8.
Она — **состояние агента, а не поле `AgentConfig`**: конфиг заморожен и
описывает вызов модели, а стратегию переключают на живом агенте, не теряя
диалог. Протокол `ContextStrategy` объявлен в самом `context.py` (в отличие от
`HistoryStore`), потому что стратегия обменивается с агентом своими типами —
`ContextTask` и `StrategyState`; иначе `context.py` пришлось бы импортировать
`agent.py` и лист графа перестал бы быть листом.

Хранилище подключено к агенту через протокол `HistoryStore` (`load`/`save` по
`session_id`), объявленный в `agent.py` ещё на дне 6: день 7 подставил в эту
точку реализацию, не меняя публичный контракт агента. Туда же встанет SQLite,
когда появится VPS и несколько пользователей.

**Checkpoint и ветка (день 10)** — операция над самой историей, а не
стратегия и не зависимость агента: `Agent.save_checkpoint()` и `Agent.fork()`
работают со стеком сообщений и реестром напрямую. Ветка собирается через
параметр `seed` конструктора — тем же кодом (`_normalize_messages()`,
`_load_context()`), которым агент читает файл сессии, — чтобы в реестр не
попал недособранный агент (правило дня 6: регистрация последним шагом
конструктора). `branch_family()`/`branch_parent()` в `agent.py` вычисляют
семейство веток по происхождению агентов в реестре, а не по ссылкам,
хранимым у родителя.

Спецификации и промпты исполнителю лежат в `docs/` и `prompts/`
(`docs/TooManyRules — День 18 спецификация.md`,
`docs/TooManyRules — День 17 спецификация.md`,
`docs/TooManyRules — День 16 спецификация.md`,
`docs/TooManyRules — День 15 спецификация.md`,
`docs/TooManyRules — День 15 проверка переходов.md`,
`docs/TooManyRules — День 14 спецификация.md`,
`docs/TooManyRules — День 14 проверка инвариантов.md`,
`docs/TooManyRules — День 13 спецификация.md`,
`docs/TooManyRules — День 13 проверка состояния задачи.md`,
`docs/TooManyRules — День 12 спецификация.md`,
`docs/TooManyRules — День 11 спецификация.md`,
`docs/TooManyRules — День 11 проверка слоёв памяти.md`,
`docs/TooManyRules — День 10 спецификация.md`,
`docs/TooManyRules — День 10 сравнение стратегий.md`,
`docs/TooManyRules — Неделя 2 архитектура.md`, `prompts/day10-prompt.md`).
Дни 11-18 обошлись без ролевого промпта исполнителю; у дней 16-18 формат
«видео + код», поэтому документа проверки нет — факты реального подключения
записаны комментарием у `BGG_MCP`, реального прогона дня 17 — у `FAQ_MCP`,
дня 18 — у `FAQ_WATCH` в `presets.py`.

## Запуск локально

```bash
./run.sh
```

Скрипт сам создаёт `src/.venv` (если его ещё нет), ставит зависимости и
запускает `src/app.py` на http://127.0.0.1:7860

Перед первым запуском нужно один раз положить API-ключ в `src/.env` — см.
шаг 2 ниже; без него приложение запустится, но на первый же вопрос ответит
понятной ошибкой вместо ответа модели.

Необязательно (день 16): для блока MCP нужен собранный бинарник bgg-mcp в
`~/.local/bin/bgg-mcp` — см. [«Сборка сервера»](#сборка-сервера). Без него
приложение работает, а кнопка MCP покажет сбой стадии «запуск».

Свой сервер FAQ (день 17) собирать не нужно — он запускается тем же Python из
`src/.venv`; нужен доступ к сайту издателя. Без сети приложение работает, а
ходы с инструментами показывают ошибку инструмента «сайт FAQ недоступен».

Сторож FAQ (день 18) запускается отдельно, в своём терминале, и работает без
приложения:

```bash
./run.sh faq-watch
```

Без него приложение работает как на дне 17: агент получает инструменты FAQ, а
блок «Сторож FAQ» показывает сбой стадии «соединение». Подробности — в разделе
[«Сторож FAQ»](#сторож-faq-день-18).

### Пошагово вручную

Все команды выполняются из папки `src/` — код и виртуальное окружение
находятся там, а не в корне репозитория.

1. Перейти в `src/` и установить зависимости (Python 3.12+, проверено на 3.14):

   ```bash
   cd src
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```

   Если системный Python старый, свежий ставится одной командой через [uv](https://docs.astral.sh/uv/):
   `uv python install 3.14 --default`.

2. Получить API-ключ DeepSeek на https://platform.deepseek.com
   и положить его в файл `.env` внутри `src/`:

   ```bash
   echo 'DEEPSEEK_API_KEY=...' > .env
   ```

   `.env` не коммитится в репозиторий (см. `src/.gitignore`).

3. Запустить (из папки `src/`, с активированным `.venv`):

   ```bash
   python app.py
   ```

   и открыть в браузере http://127.0.0.1:7860

## Неделя 1 — `app_week1.py`

Дни 1-5 были сравнительными экспериментами (A vs B, четыре способа
рассуждения, три температуры × три повтора, матрица «модель × thinking»),
и каждый жил своей вкладкой. Этот код заморожен отдельным запускаемым
артефактом и больше не редактируется:

```bash
./run.sh week1
```

Запускается тот же интерфейс с пятью вкладками, что и на теге `day05`.
Артефакт самодостаточен: он ничего не импортирует из нового кода и ничего
ему не отдаёт — поэтому дублирование системного промпта, констант DeepSeek
и таблицы цен между ним и `agent.py`/`presets.py` оставлено намеренно.
