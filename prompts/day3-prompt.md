Роль: ты — Python-разработчик. Реализуй день 3 проекта TooManyRules строго по спецификации из репозитория. Добавь новый функционал в уже существующий интерфейс (`src/app.py`) — не создавай отдельный скрипт.

Сначала прочитай:
- `docs/TooManyRules — День 3 спецификация.md` — точное ТЗ на сегодня: одна и та же неоднозначная аналитическая задача по правилам прогоняется через DeepSeek API четырьмя способами (прямой ответ; «решай пошагово»; модель сама формулирует промпт, затем он используется отдельным запросом; консилиум из трёх экспертных персон), черновики системных промптов для каждого способа, требования к вкладке «День 3», критерии приёмки и раздел «Что сознательно НЕ делаем на этом шаге».
- `src/app.py` и спецификации дней 1–2 — уже реализованный интерфейс. Базовый ролевой системный промпт (константа `SYSTEM_PROMPT`) используется как есть в Способе 1 и как основа, к которой добавляются инструкции способов 2–4; сами вкладки «День 1» и «День 2» не трогать, новая вкладка «День 3» добавляется рядом по тому же паттерну (`gr.Tab` внутри существующего `gr.Blocks`).
- `docs/TooManyRules — спецификация проекта.md` — общий контекст проекта (нужен для понимания, зачем и куда это движется; сама реализация ограничена только заданием дня 3).

Обрати внимание на два момента при чтении спецификации дня 3:
- От кода требуется только вывести все четыре подписанных ответа рядом друг с другом (по одному блоку на способ), чтобы их можно было визуально сопоставить. Оценка того, какой способ точнее — это ручное суждение автора по бумажной книге правил (озвучивается или показывается курсором в видео), код её не вычисляет и не подсвечивает — не реализуй никакой автоматической метрики или подсветки «лучшего» ответа, это явно исключено из объёма (§7 спецификации).
- Способ 3 требует двух последовательных вызовов API (сначала генерация промпта, потом сам ответ) — оба вызова должны быть видны в логах/интерфейсе так же, как это сделано для дня 2 (`logging` в stdout).

Не расширяй объём задачи сверх файла дня 3 — раздел «Что сознательно НЕ делаем на этом шаге» в нём обязателен к соблюдению.

Если что-то в спецификации неоднозначно — задай уточняющий вопрос, не додумывай самостоятельно.
