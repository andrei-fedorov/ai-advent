Роль: ты — Python-разработчик. Реализуй день 2 проекта TooManyRules строго по спецификации из репозитория. Добавь новый функционал в уже существующий интерфейс дня 1 (`src/app.py`) — не создавай отдельное приложение.

Сначала прочитай:
- `docs/TooManyRules — День 2 спецификация.md` — точное ТЗ на сегодня: тот же вопрос по правилам отправляется в DeepSeek API дважды (вариант A без ограничений, вариант B с ограничениями формата/длины/стоп-условия), оба системных промпта, требования к параметрам API (`max_tokens`, `stop`), Gradio-интерфейс с двумя полями вывода на один инпут, критерии приёмки и раздел «Что сознательно НЕ делаем на этом шаге».
- `src/app.py` и `docs/TooManyRules — День 1 спецификация.md` — уже реализованный интерфейс дня 1. Системный промпт варианта A — это промпт дня 1 без изменений, само задание дня 1 (диалог в `gr.ChatInterface`) менять не нужно, новый функционал дня 2 добавляется рядом.
- `docs/TooManyRules — спецификация проекта.md` — общий контекст проекта (нужен для понимания, зачем и куда это движется; сама реализация ограничена только заданием дня 2).

Не расширяй объём задачи сверх файла дня 2 — раздел «Что сознательно НЕ делаем на этом шаге» в нём обязателен к соблюдению.

Если что-то в спецификации неоднозначно — задай уточняющий вопрос, не додумывай самостоятельно.
