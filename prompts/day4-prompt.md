Роль: ты — Python-разработчик. Реализуй день 4 проекта TooManyRules строго по спецификации из репозитория. Добавь новый функционал в уже существующий интерфейс (`src/app.py`) — не создавай отдельный скрипт.

Сначала прочитай:
- `docs/TooManyRules — День 4 спецификация.md` — точное ТЗ на сегодня: один и тот же вопрос по правилам Too Many Bones прогоняется через DeepSeek API при трёх значениях `temperature` (0 / 0.7 / 1.2), с тремя повторами на каждое значение (итого 9 вызовов) — повторы нужны, чтобы было видно разнообразие ответов при одной и той же температуре, а не только их содержание. Требования к вкладке «День 4», параметры запросов и критерии приёмки — там же.
- `src/app.py` и спецификации/промпты дней 1-3 — уже реализованный интерфейс. Базовый системный промпт (`SYSTEM_PROMPT`) используется как есть, без изменений; вкладки «День 1», «День 2», «День 3» не трогать, новая вкладка «День 4» добавляется рядом по тому же паттерну (`gr.Tab` внутри существующего `gr.Blocks`).
- `docs/TooManyRules — спецификация проекта.md` — общий контекст проекта (нужен для понимания, зачем и куда это движется; сама реализация ограничена только заданием дня 4).

Обрати внимание на два момента при чтении спецификации дня 4:
- Единственный варьируемый параметр между всеми 9 вызовами — `temperature`; системный промпт, вопрос пользователя и остальные параметры API (без `max_tokens`/`stop` из дня 2) одинаковы во всех вызовах.
- От кода требуется только вывести все 9 подписанных ответов (3 температуры × 3 повтора) рядом друг с другом для визуального сопоставления и залогировать каждый вызов в stdout (temperature, номер повтора, finish_reason, ответ) — как это сделано для дней 2-3. Сравнение по точности/креативности/разнообразию и вывод, для каких задач подходит какая температура — это ручное суждение автора, озвучиваемое в видео; не реализуй автоматическую метрику или подсветку «лучшего» варианта — это явно исключено из объёма (§7 спецификации).

Не расширяй объём задачи сверх файла дня 4 — раздел «Что сознательно НЕ делаем на этом шаге» в нём обязателен к соблюдению.

Если что-то в спецификации неоднозначно — задай уточняющий вопрос, не додумывай самостоятельно.
