# TooManyRules — архитектура недели 2 (дни 6-10)

Документ фиксирует перестройку кодовой базы на границе недель 1 и 2. Спецификации дней 6-10 опираются на него и не повторяют этих решений.

## 1. Почему меняем подход

Неделя 1 («Основы LLM») состояла из **сравнительных экспериментов**: A vs B, четыре способа рассуждения, три температуры × три повтора, матрица 2×2. Каждый день давал самостоятельную витрину, которую нужно было видеть целиком и рядом с предыдущими — вкладка на день была для этого правильной формой.

Неделя 2 («База про агентов») устроена иначе: дни **наращивают одну и ту же сущность** — агент (день 6) → память между запусками (день 7) → работа с токенами (день 8) → сжатие истории в сводку (день 9) → три стратегии управления контекстом без summary и переключатель между ними (день 10). Вкладка на день здесь дала бы пять почти одинаковых чатов, а день 7 вкладкой не показывается в принципе: его демонстрация — «убили процесс, запустили заново, диалог продолжается».

Поэтому на неделе 2:

- код недели 1 замораживается отдельным запускаемым артефактом;
- неделя 2 разрабатывается классически — один растущий `app.py`, коммит и тег на каждый день, возврат назад через git;
- роль «что нового сегодня» берёт на себя не вкладка, а **дебаг-панель**, которая прирастает вместе с агентом (день 6 — конфиг, стек, метрики; день 7 — файл истории и факт восстановления; дальше — сжатие и токены).

## 2. Заморозка недели 1

- `src/app.py` → `src/app_week1.py`. Дальше не редактируется; в шапке файла — комментарий, что это замороженный артефакт недели 1 (дни 1-5, вкладки).
- Файл **самодостаточен**: не импортирует новые модули недели 2 и ничего им не отдаёт. Дублирование системного промпта, констант и таблицы цен с новым кодом — осознанная цена заморозки: артефакт должен запускаться, даже когда новый код уедет далеко вперёд.
- Запуск: `./run.sh` — текущее приложение, `./run.sh week1` — артефакт недели 1 (единственное изменение в `run.sh`: необязательный аргумент выбирает файл запуска, по умолчанию `app.py`).
- Настоящий архив всё равно git: тег `day05` — состояние на конец недели 1. Файл-артефакт нужен для того, чтобы показать неделю 1 **не переключая ветки**, в том числе на видео.

## 3. Структура кода недели 2

| Файл | Назначение |
| --- | --- |
| `src/agent.py` | Сущность агента: `AgentConfig`, `AgentReply`, `Agent`, статистика. **Единственное место в проекте, где происходят вызовы LLM API.** Не знает ни про Gradio, ни про Too Many Bones |
| `src/presets.py` | Конфиги агентов TooManyRules: системный промпт проекта, модели, параметры, набор пресетов |
| `src/storage.py` | Появляется на дне 7: история диалога в JSON-файлах по сессиям (`src/data/sessions/`), загрузка и сохранение через протокол `HistoryStore` |
| `src/tokens.py` | Появляется на дне 8: оценка токенов без обращения к API, разложение запроса по частям, контекстные окна моделей, бюджет контекста. Чистые функции, ни сети, ни состояния |
| `src/context.py` | Появляется на дне 9: стратегии управления контекстом — что из истории уходит в модель. Протокол `ContextStrategy`, «Вся история» и «Сводка + последние N»; день 10 добавляет сюда же свои стратегии. Ни сети, ни Gradio, ни Too Many Bones |
| `src/app.py` | Только интерфейс на Gradio: чат + дебаг-панель. LLM-логики не содержит |
| `src/app_week1.py` | Замороженный артефакт недели 1, вне графа импортов нового кода |

Направление зависимостей строго одностороннее: `app.py` → `presets.py` → `agent.py` → `tokens.py`, и `app.py` → `storage.py`. Протокол хранилища объявлен в `agent.py`, но реализация его не импортирует — протокол структурный, наследовать его не требуется. Циклов быть не должно; `agent.py` импортирует из проекта только листья графа, которые сами не импортируют ничего: `tokens.py` (с дня 8) и `context.py` (с дня 9). Протокол стратегии, в отличие от протокола хранилища, объявлен в самом `context.py` — вместе с типами, которые стратегия конструирует и отдаёт агенту (обоснование — спецификация дня 9, §4).

Что переносим из недели 1: `SYSTEM_PROMPT`, константы DeepSeek (`base_url`, модели, `extra_body` для `thinking`), таблицу цен и функцию оценки стоимости, паттерн логирования вызовов в stdout, и — как значения пресетов — ограничения дня 2 (`JSON`-промпт, `max_tokens`, `stop`) и параметры дней 4-5 (`temperature`, `pro` + `thinking`).

Что **не** переносим: вкладки, сравнительные функции (`compare_variants`, `run_day3`, `run_day4`, `run_day5`), валидацию JSON дня 2, подсветку минимумов дня 5, мета-промпт и консилиум дня 3. Всё это остаётся жить в `app_week1.py`.

## 4. Точки роста под известные и ожидаемые дни

Архитектура должна делать так, чтобы каждый следующий день менял **одно место**, а не расползался по коду:

- **День 7 (сохранение контекста, задание известно).** В `agent.py` объявляется протокол хранилища (`load`/`save` по `session_id`); агент принимает его необязательным параметром конструктора. День 6 работает с `None` (память процесса), день 7 добавляет реализацию в `storage.py` — **публичный контракт агента при этом не меняется**: подставляется реализация, а не переделывается агент. Подробности — `docs/TooManyRules — День 7 спецификация.md`.
- **День 8 (работа с токенами, задание известно).** `AgentReply` уже несёт `prompt`/`completion`/`total` токены и стоимость, агент уже копит их за время жизни — счёт до запроса, бюджет контекста и рост по ходам надстраиваются над готовыми метриками. Оценка токенов уезжает в отдельный лист графа (`tokens.py`), агент считает бюджет по результату той же сборки сообщений, которая уходит в API. Подробности — `docs/TooManyRules — День 8 спецификация.md`.
- **День 9 (сжатие истории, задание известно).** Сборка списка сообщений для запроса живёт в одном приватном методе агента (`_build_messages()`) — туда подключается стратегия контекста, а не размазывается по вызовам, и меряется тем же счётчиком токенов, что появился на дне 8. Стратегия встаёт третьей зависимостью агента после конфига и хранилища и, как хранилище, остаётся необязательной; история при этом остаётся полной — сжимается запрос, а не память. Подробности — `docs/TooManyRules — День 9 спецификация.md`.
- **День 10 (три стратегии без summary, задание известно).** Sliding window и sticky facts — ещё две реализации того же протокола `ContextStrategy`, попадающие в тот же переключатель, что появился на дне 9. Ветки диалога под этот протокол не ложатся: это операция не над сборкой запроса, а над историей, и делать её предполагается сессиями с общим префиксом (§7). Решение за спецификацией дня 10.
- **Дальше (недели 4-7).** Агент ничего не знает про Gradio, поэтому его можно поднять за HTTP (REST-сервис, телеграм-бот) или переключить на локальную модель через `base_url` в конфиге, не трогая логику. Для локальной модели цена в долларах и размер окна неизвестны — и `estimate_cost_usd()`, и таблица окон дня 8 возвращают `None`, а панель показывает «н/д».

Абстракции под ещё не начатые дни заранее **не пишутся** — фиксируются только места, куда они встанут: день 9 вводит протокол стратегий с двумя реализациями, но ни одной заготовки под стратегии дня 10 в нём нет.

## 5. Конвенции недели

- В конце каждого дня — коммит и тег `day06`, `day07`, …: в таблицу челленджа идёт ссылка на тег, а не на «последний main», который к моменту проверки уже уедет вперёд.
- `README.md` описывает **текущее** состояние приложения плюс короткий раздел «неделя 1 — `app_week1.py`, запускается через `./run.sh week1`».
- На каждый день, как и раньше: спецификация в `docs/` и промпт исполнителю в `prompts/`.
- Дебаг-панель — обязательная часть каждого дня недели: то, что добавилось сегодня, должно быть в ней видно.

## 6. План по дням

| День | Тема | Статус |
| --- | --- | --- |
| 6 | Первый агент: `agent.py`, новый `app.py`, заморозка недели 1 | задание известно, реализовано, тег `day06` |
| 7 | Сохранение контекста: история в JSON-файлах по сессиям, восстановление агентов при старте процесса | задание известно, реализовано, тег `day07` |
| 8 | Работа с токенами: счёт до запроса и после, бюджет контекста, рост по ходам, переполнение | задание известно, реализовано, тег `day08` |
| 9 | Управление контекстом: последние N сообщений как есть, остальное — сводка; сводка хранится отдельно и переживает перезапуск | задание известно, спецификация написана |
| 10 | Управление контекстом: три стратегии без summary (sliding window, sticky facts, ветки) и переключатель между ними | задание известно, спецификация не написана |

## 7. Открытые решения

- ~~**JSON или SQLite на дне 7.**~~ **Решено** в `docs/TooManyRules — День 7 спецификация.md` (§2.1): интерфейс хранилища + реализация на JSON-файлах, по файлу на сессию в `src/data/sessions/`. Для одного локального пользователя этого достаточно, и на видео это нагляднее (видно сам файл с `messages`). SQLite из общей спецификации проекта берём, когда появится VPS и несколько сессий/пользователей — протокол `HistoryStore` позволит подменить реализацию без переписывания агента.
- **Ветки диалога на дне 10.** Две ветки от одного checkpoint'а — это две истории, а не два способа собрать запрос, поэтому в протокол стратегий они не встают. Напрашивается ветка как отдельная сессия с общим префиксом (`storage.create_session()` уже выдаёт номера, реестр агентов уже умеет переключаться между агентами, а `SessionInfo` можно дополнить ссылкой на родителя и точку ветвления) — но решение принимает спецификация дня 10; день 9 обязан только не мешать любому из вариантов.
- **Стриминг ответа.** Новый чат работает без стриминга: обычный вызов даёт полные метрики (время, токены, стоимость, `finish_reason`) на каждый ход, а они — главное содержимое дебаг-панели. Стриминг вернём, когда он станет нужен продукту, а не демонстрации.
